# 2026-09-18

- 收口 destroy 竞态防御系列（共 17 个提交，自 064cbf9 起）：MSE/WebCodecs 初始化与 seek 完成回调、直播追赶、endOfStream、demuxer/pipeline 失败分类均接入 `_lifecycleGeneration` 代数守卫 + `isCurrent()` 检查，destroy 后的陈旧回调不再触达新管线；最后一笔 `a6de832 fix: stop stale WebCodecs live catch-up after destroy`。
- 第一百零七波（audit-79 D 重构收敛，owner 已点头）：BitReader×4 → core 收敛 2 处（ts/src/bits.js、flv/src/bits-lite.js 真删真迁移，`readBits(48)` 丢弃型消费改 `skipBits`，ts/aac.js `r.pos/r.bit` 适配 `bitPosition`）；BitWriter×2 → core/bit-reader.js 统一实现（flac 删本地类改 re-export）；buildEsds×2 → 新建 core/src/esds.js 唯一实现（mp4/box-builder.js re-export 维持公共面并删死导出 buildBtrt，hls/fmp4-muxer.js 以 `{maxBitrate:128000, avgBitrate:128000}` 参数透传保字节级兼容）。flac BitReader 刻意保留（byteOffset+bitLimit 相对窗口域、受测试固化的中文错误文案、FLAC 专属读法），4→2 而非 4→1，属正当差异而非漂移。
- 新增 `core/__tests__/bit-reader-exp-golomb.test.js` 4 例收敛回归（readUE/readSE 同语义含 >32 前导零抛 PARSE_ERROR、readFlag/alignByte 别名、BitWriter 位级读写/往返/动态扩容、merge 位级连续拼接）；`hls/__tests__/ts-remux.test.js` 追加 core 直出与 hls 包装的 deepEqual 字节级一致断言。
- 全仓回归 2496/2496（--test-concurrency=4）通过；lint 479 文件 0 警告；`npm run check` 16/16 模块达标；结构层与运行时 §2.4 双契约审计通过；`git diff --check` 通过。
- 台账 `docs/review/iteration-backlog.md` 候选池 P3 重构收敛项已标记完成并登记第一百零七波条目。
- 第一百零八波（env 层可测化续作，迭代扫描建议项）：flac/player.js 38.0→98.8%、mp4/webcodecs-pipeline.js 40.6→97.2%，env 层均分 79.3%→86.2%。新增 `flac/__tests__/player-smart.test.js`（+6：loadFlacSmart 三路径 / createFlacPlayer 探测 / WAV 封装字节级）与 `mp4/__tests__/webcodecs-pipeline.test.js` 扩展（+7：Fake WebCodecs 全局下管线类 supported/start/fromSampleIndex/decode 抛错/背压/reset/close/重复 start 全分支）。手法沿用仓库既有 withGlobals try/finally + 注入式 stub，未引入 Playwright。全仓 2509/2509（+13）、lint 480 文件、check 16/16、双契约审计 0/PASS。
- 第一百零九波（env 层可测化第三批 + 修复 2 真实缺陷）：hls/player.js 40.1→94.1%，env 层均分 86.2%→89.4%。新增 `hls/__tests__/player-pipeline.test.js`（+12 例，注入 loader/mse/transmuxer + 真实 m3u8 解析 + node:test mock timers）。**缺陷修复（hls/src/player.js）**：①`computeResumeIndexBySn` 未导入——直播轮询/切档锚点续播一进入即 ReferenceError 被伪装成 network 错误，属潜伏回归（utils 版有直测但 player 路径零覆盖）；②`_loadMediaPlaylist` 不更新 `playlistUrl`——直播 ABR 切档后轮询仍 reload 旧档位清单（原 loadSource master 分支的手工赋值掩盖了缺漏）。全仓 2521/2521（+12）、lint 481 文件、check 16/16、双契约审计 0/PASS。
- 第一百一十波（env 层可测化收官）：hls/mse-controller.js 53.0→97.3%，env 层均分 89.4%→92.0%。新增 `hls/__tests__/mse-controller.test.js`（+14 例，Fake MediaSource/SourceBuffer/URL 注入）：attach 双构造器/sourceerror、append 队列串行化与 QuotaExceeded 双路径分类、remove abort、trim 保留窗口、currentBufferSeconds 回退链、finalize/endOfStream/destroy 全链与幂等。全仓 2535/2535（+14）、lint 482 文件、check 16/16、双契约审计 PASS。三波累计 env 层 79.3%→92.0%（+12.7pp），flac/mp4/hls 三模块 player 层全部纳入可测范围。
- 第一百一十一波（wav 调度层可测化 + 修复首播断流缺陷）：wav/player.js 69.9→100%（335/335），env 层均分 92.1%→93.7%。新增 `wav/__tests__/wav-player-smart.test.js`（+9 例，Fake WebAudio 注入 globalThis + node:test mock timers 驱动真实推送循环 + WriteTracker 校验分段连续性）：首播循环存活至 eof、暂停恢复续启、ended 重播单次预填充、播放中 seek 游标采纳、高水位节流、上行消息全分支、ensureGraph/stop/resume 全链。**缺陷修复（wav/src/player.js）**：`#prefill` 在 `state==='ready'` 时执行而推送循环守卫要求 `playing`——循环预填充阶段即自灭，首播播完 8192 帧（~170ms）后永久断流、eof/ended 永不触发，暂停恢复同样无人续推。修复=先置 playing 再装载 + ended/paused 走 `#maybePushRest` 续启；配套 `#maybePushRest` 改「已推送仅更新游标」+ 删 `seek()` 无条件 `#pushGen++`（二者叠加曾致播放中 seek 后死循环）。全仓 2544/2544（+9）、lint 483 文件、check 16/16、双契约审计 PASS。
- 第一百一十二波（core 音频输出契约实现全链可测化）：core/audio-worklet-player.js 63.7→99.3%，env 层均分 93.7%→95.8%。扩展 `core/__tests__/core-audio-worklet-env.test.js`（+7 例，Fake WebAudio 注入 globalThis）：init 装载链与幂等、push 时序守卫/空数据早退/Transferable、play/pause/resume 状态机与锚点、currentTimeUs 整数 µs 主钟（契约 §7）、上行 stats/underrun 消息、clearBuffer、destroy 全链幂等、createAudioOutput channels/channelCount 别名等价。无缺陷发现（实现与契约一致）。全仓 2551/2551（+7）、lint 483 文件、check 16/16、双契约审计 PASS。
- 第一百一十三波（CMAF WebCodecs 直解路线可测化）：cmaf/webcodecs.js 68.2→100%（179/179），env 层均分 95.8%→97.7%（未覆盖行 175→）。新增 `cmaf/__tests__/cmaf-webcodecs-env.test.js`（+8 例，Fake VideoDecoder/AudioDecoder/EncodedVideoChunk 同时注入 globalThis 与 window + fMP4 构造器真实 fixture）：open 配置推导、isConfigSupported 拒绝、appendChunk 分轨与 ticks→µs 换算、trackId===1 视频启发式（非 1 轨回退音频解码器，断言固化现行为）、decode 抛错隔离、close 四态、output·error 回调连线。无缺陷发现（实现与契约 §6 一致）。全仓 2559/2559（+8）、lint 484 文件、check 16/16、双契约审计 PASS。
- 第一百一十四波（core 编排层 load/默认管线/泵错误路径补测）：core/player.js 91.6→99.7%（全量 641/643），env 层均分 97.7%→98.0%。新增 `core/__tests__/core-player-load-defaults.test.js`（+14 例，Fake WebCodecs/MSE 部件注入）：detectForMedia 合并探测、默认管线工厂三态（WC/MSE/null）、输入形态分发（URL/Blob/非法）、load 重入、caps 探测期间销毁 ABORTED、管线工厂迟到成功 destroyLate 回收、管线工厂当前代失败清理、playing 切轨成功续泵、seeking 中 play 拒绝、预缓冲/泵 readSample 抛错路径、buffered 合成与 durationUs=null。无缺陷发现。全仓 2573/2573（+14）、lint 485 文件、check 16/16、双契约审计 PASS。
- 第一百一十五波（hls player 残余分支补测）：hls/player.js 94.1→98.78%（567/574），env 层均分 98.0%→98.47%。新增 `hls/__tests__/player-gaps.test.js`（+9 例，门控 loader + bufferfull mock timers + AES-128 fake decrypter + 一次性 reportLoad wrapper）：两级装载失败、缓冲已满暂缓恢复、AES-128 解密链路、ABR 切档重载、音频-only 固化、trim 失败静默、自动播放拦截、直播轮询失败自愈、无 video 控制接口。发现疑似不可达防御分支 2 处（hls/src/player.js:245-250 EXT-X-MAP 兜底建 SB、:362 audioOnlyHint+空 codecs），登记待 owner 裁决不删。全仓 2582/2582（+9）、lint 486 文件、check 16/16、双契约审计 PASS。
- 第一百一十六波（SPS 深分支 + b64 双端回退补测）：flv/codec-info.js 83.1→100%（201/201，新增 `flv/__tests__/codec-info-sps.test.js` +11 例，core BitWriter 程序化构造 H264/HEVC SPS：高档位 chroma/scaling matrix 12 组列表、pocType 0/1、frame_mbs_only=0 高度×2、HEVC conformance/多层/截断）；rtsp/b64.js 80.0→100%（30/30，rtsp-sdp-edge.test.js +5 例，withGlobals 驱动 atob/btoa/Buffer 双端回退与抛错分支）。无缺陷发现。全仓 2599/2599（+17）、lint 487 文件、check 16/16、双契约审计 PASS。
- 第一百一十七波（ts nalu 深分支补测）：ts/nalu.js 87.9→100%（379/379，nalu.test.js +9 例，复用 wave116 BitWriter SPS 构造器：类型判定四助手、H264 高档位 chroma/scaling matrix/pocType、HEVC conformance、parseHevcSpsConfig 全字段与多层降级）。无缺陷发现。全仓 2608/2608（+9）、lint 487 文件、check 16/16、双契约审计 PASS。
- 第一百一十八波（flv demuxer 残余分支补测）：flv/flv-demuxer.js 90.2→97.72%（601/615，新增 flv/__tests__/flv-demuxer-gaps.test.js +20 例：ChunkSource 三降级/metadata 回填/waiter 交付、泵 EOF 四形态、迭代器内泵（>64KB fixture）、push/start、工厂 fetch/Blob/DataSource）。无产品缺陷；两条时序要点已固化到台账：open() 让一拍微任务后才注册监听（ChunkSource 测试须 setImmediate 再喂数据，否则事件循环排空级联取消）、泵 read 返回 < want 即 EOF。全仓 2628/2628（+20）、lint 488 文件、check 16/16、双契约审计 PASS。
- 第一百一十九波（rtsp H265 分片容错 + 出口委托补测）：rtsp/depacketize-h265.js 86.2→100%（123/123，depacketize-h265-gaps.test.js +5 例：reset/乱序丢弃/跳空计数/FU 短包/FU 不连续 abortFu 计数）；ts/emitter.js 90.2→100%（emitter-edge.test.js +3 例：off/异常转发 error/error 处理器自抛降级 console.error）；flv/index.js 83.3→95.83%（+2 例 index 委托；残余 2 行为 probe try/catch 防御分支，公共路径不可达，登记不硬造）。无产品缺陷。全仓 2638/2638（+10）、lint 490 文件、check 16/16、双契约审计 PASS。
- 第一百二十波（core HTTP Range 源 + hls 转封装适配层补测）：core/http-range-source.js 89.9→99.03%（206/208，http-range-gaps.test.js +9 例：无 fetch 构造抛错/open 退路失败两类/body.cancel 吞并/read 越界/非 206·200/无视 Range 返 200 双路径/206 短响应）；hls/transmuxer.js 87.8→98.13%（transmuxer-gaps.test.js +2 例：isInit 直通、真实 TS 分片 transmuxed + reset/destroy 重建；残余 tsAvailable catch 不可达）。无产品缺陷。全仓 2649/2649（+11）、lint 492 文件、check 16/16、双契约审计 PASS。
- 第一百二十一波（core registry 自动选路 + ts 出口委托补测）：core/registry.js 91.7→100%（145/145，registry-gaps.test.js +5 例：detail 聚合异常记 0/命中名无注册模块携带 hit/detectFromUrl 无 fetch·头请求失败·cancel 同步抛错·不可识别）；ts/index.js 88.4→97.1%（index-delegates.test.js +2 例委托；残余 2 行 probe try/catch 防御与 flv 同型，登记不硬造）。无产品缺陷。全仓 2656/2656（+7）、lint 494 文件、check 16/16、双契约审计 PASS。
- 第一百二十二波（hls ChunkSource 出口补测）：hls/data-source.js 89.5→100%（162/162，data-source-gaps.test.js +4 例：defaultFetchImpl 双路径/EXT-X-MAP pendingInit/detectContainer fMP4·unknown/close 语义=不启动后续分片）。无产品缺陷。全仓 2660/2660（+4）、lint 495 文件、check 16/16、双契约审计 PASS。
- 第一百二十三波（hls 解密器 + mp4 出口 + flv emitter 残余分支补测）：hls/decrypter.js 93.1→100%（202/202，decrypter-gaps.test.js +6 例：defaultKeyLoader 三路径、_decryptSoftware 长度/坏密钥两错、destroy 清缓存）；mp4/index.js 92.0→100%（index-gaps.test.js +2 例：read 抛错归一 PROBE_FAILED、垃圾字节）；flv/emitter.js 93.8→100%（emitter-edge.test.js +2 例，与 ts 同构镜像）。无产品缺陷。全仓 2670/2670（+10）、lint 498 文件、check 16/16、双契约审计 PASS。
- 第一百二十四波（wav/flac demuxer 残余分支补测）：wav/demuxer.js 93.0→98.76%（demuxer-gaps.test.js +6 例：probe 防御/getBufferedRanges 空/seek 两级 STATE_ERROR/迭代读抛错 error 态/abort 不污染 error 面；残余 seek 数学体 catch 不可达）；flac/demuxer.js 94.0→99.29%（demuxer-gaps.test.js +6 例：probe 防御/PADDING 70000B 逼出 cap 重读/destroy 后 seek/建索引抛错 error 态；残余 catch(e){throw e} 直通不可达）。无产品缺陷。全仓 2682/2682（+12）、lint 500 文件、check 16/16、双契约审计 PASS。
- 第一百二十五波（core ByteStream/ByteWriter + mp4 box-builder 残余分支补测）：core/byte-stream.js 94.7→100%（397/397，byte-stream-gaps.test.js +8 例）；mp4/box-builder.js 96.1→98.17%（box-builder-gaps.test.js +4 例：buildFree/buildHvcC/buildStsd hvc1/未知类型 notSupported；残余 largesize >4GB 与 findTrunDataOffset 内部不可达登记）。注意：sourceError/notSupported 是工厂函数，断言用 e.code；VisualSampleEntry 宽高为 U16 非 16.16 定点；stsd entry 起点在 16（头8+ver/flags4+entry_count4）。无产品缺陷。全仓 2694/2694（+12）、lint 502 文件、check 16/16、双契约审计 PASS。
- 第一百二十六波（core Demuxer 基类残余分支补测）：core/demuxer.js 94.4→100%（448/448，demuxer-base-gaps.test.js +11 例，双轨 toy demuxer：非法迁移/终态吞错/无 source/未实现钩子类名提示/默认 _doSeek/attach 时序/seek 非法入参/直播暂停 sample 事件照发/close 抛错吞并/getTracks·getTrack 过滤）。无产品缺陷。全仓 2705/2705（+11）、lint 503 文件、check 16/16、双契约审计 PASS。
- 第一百二十七波（mp4 demuxer/box-parser 残余分支补测）：mp4/box-parser.js 97.2→100%（636/636）；mp4/demuxer.js 96.0→99.36%（623/627，demuxer-gaps.test.js +15 例：probe wide/0.82、无 moov、顶层 largesize 与 size=0、截断 largesize 头、enca NOT_SUPPORTED、hvc1 轨、moof 无 mdat、分片 seek 二分续读、双轨渐进 seek _locateResumeIndex、expandSampleTable 兜底 0、parseTkhd v1、parseMvex mehd v1、parseBoxByType 分派、collectChildren 零子 box 重抛）。残余 4 行登记：probe catch 84-86 防御分支、169-170 为 c8 行归属伪影（直接调 _scanTopLevel 实验验证不翻转）。无产品缺陷。全仓 2720/2720（+15）、lint 504 文件、check 16/16、双契约审计 PASS。

## 第一百二十八波（ts-demuxer 残余分支补测 + 修复休眠缺陷）
- ts-demuxer.js 90.5→99.25%（532/536）：新增 ts/__tests__/ts-demuxer-gaps.test.js +15 例（数据源归一化 Blob/非法、_feed/engine error 转发、chunk 模式全流程含 _waiters 405 挂起、泵失败三形态、start() pause/resume 与消费循环错误捕获、_doSeek 直调、工厂四路径）。
- **修复休眠缺陷**：ts-demuxer.js:473 `seekUnsupportedTs` 未定义 → `seekUnsupported`（公开 API 因 seekable=false 基类短路，直调 _doSeek 才触发 ReferenceError）。
- 关键语义固化：TS 引擎按 AU 边界惰性出样——PES 到达不立即出样，由下一 PES 或 flush 冲出；参数集 PES 亦产样本（keyframe=false）；音频 ADTS config 在 AU 完成时建立。_emitMetadata durationMs 取各轨 lastDtsRaw 全局最大值。
- 残余 6 行登记不硬造：demuxer probe catch 68-69、308-309（_pumpOnce false ⟹ _sourceEof，不可达）、engine 三处兜底 catch 196/400/555。
- 门禁：全仓 2735/2735（+15）、lint 505 文件 0 警告、check 16/16、双契约审计 PASS。

## 第一百二十九波（gateway ws-server 残余分支补测 + 修复 Buffer.constants 缺陷）
- samples/gateway/ws-server.js 80.7→98.75%（395/400）：新增 samples/gateway/__tests__/ws-server-gaps.test.js +22 例（FrameDecoder 控制帧/分片重组/64bit/超大帧拒绝、WsConnection ping-pong/teardown 异常吞噬/心跳两分支、createWsHttpServer 404/400/close 覆盖层）。
- **修复产品缺陷**：ws-server.js:140 `Buffer.constants.MAX_LENGTH` 在 Node 22 为 undefined → TypeError；改 `import { constants as bufferConstants } from 'node:buffer'`。
- 手法：fake 双工 socket（write/end/destroy 可注入失败）单元直驱 + 真实端口 net/fetch 测 HTTP 404 与 Upgrade 400；心跳超时用短周期 50ms 真实定时器。
- 残余 2 行登记不硬造：225-227（alive=false 前必先 clearInterval，不可达）、386-387（dispose 兜底，真实 socket 不同步抛错）。
- 门禁：全仓 67058/68284（+22）、lint 506 文件 0 警告、check 16/16、双契约审计 PASS。

## 第一百三十波（ts aac/psi 残余分支补测 + 死代码删除）
- ts/aac.js 93.5→99.18%（243/245）：新增 ts/__tests__/aac-gaps.test.js +4 例（假同步码、LATM 非同步/零长跳过、ASC 扫描耗尽→外层 catch、PLI=0→{asc,payload:null}）。ts/psi.js 93.4→100%（175/175）：删除死代码 readSectionFromPayload（round-1 已登记无调用方、未导出）。
- 教训①：LATM 有界扫描的接受条件（parse ch=0→2 兜底）使「窗口后段 0x00」构成合法假 ASC——构造耗尽扫描需窗口内全 1（idx 恒 15 前置跳过）。
- 教训②：c8 subset 快测若不带 --reporter=lcov，lcov.info 是旧残留，逐行残余分析必须用同一次跑的 lcov。
- 残余 2 行登记不硬造：aac.js 227-228（aot=31 ⟹ idx 14/15 前置跳过；idx≤12 不越窗 → parse 不抛，不可达）。
- 门禁：全仓 67064/68242（+4）、lint 507 文件 0 警告、check 16/16、双契约审计 PASS。

## 第一百三十一波（rtsp depacketize-h264 残余分支补测）
- rtsp/depacketize-h264.js 92.0→100%（137/137）：新增 rtsp/__tests__/depacketize-h264-gaps.test.js +4 例（reset 归零、type 0/29 忽略且作废 FU、#abortFu body、空 AU 收束）。无产品缺陷。
- 门禁：全仓 67092/68242（+4）、lint 508 文件 0 警告、check 16/16、双契约审计 PASS。

## 第一百三十二波（core nal 残余分支补测）
- core/nal.js 94.5→100%（183/183）：新增 core/__tests__/nal-gaps.test.js +5 例（空 NAL 拒绝、splitAnnexB 零拷贝、lengthSize 越界×2、avcc 尾部垃圾）。无产品缺陷。
- 门禁：全仓 67106/68242（+5）、lint 509 文件 0 警告、check 16/16、双契约审计 PASS。

## 第一百三十三波（flv tag-stream 残余分支补测 + 死守卫登记）
- flv/tag-stream.js 95.1→98.91%（183/185）：新增 flv/__tests__/tag-stream-gaps.test.js（reset 归零可重解析）。
- **登记结构性死守卫**：tag-stream.js:106-107「dataSize>128MB」不可达（dataSize 24 位上限 0xFFFFFF≈16MB）；收紧阈值会改变行为，维持现状。
- 门禁：全仓 67120/68242（+1）、lint 510 文件 0 警告、check 16/16、双契约审计 PASS。

## 第一百三十四波（hls segment-loader 残余分支补测）
- hls/segment-loader.js 94.2→100%（240/240）：新增 hls/__tests__/segment-loader-gaps.test.js +4 例（无 fetch 环境 NOT_SUPPORTED、reader.cancel 抛错吞噬、delay abort 监听/预中止两形态）。无产品缺陷。
- 门禁：全仓 67148/68242（+4）、lint 511 文件 0 警告、check 16/16、双契约审计 PASS。

## 第一百三十五波（rtsp client 残余分支补测，子代理执行模式首波）
- rtsp/client.js 87.6→100%（387/387）：新增 rtsp/__tests__/client-gaps.test.js +13 例（FakeWs 可脚本化应答 + withGlobals 注入 WebSocket/fetch；覆盖超时/网络错/保活/响应分类/sdpUrl/兜底建轨/畸形 RTP error 事件等分支；请求响应超时用 node:test mock.timers 虚拟时钟免真实等待）。
- 勘误：334-336 实为 #handleRtpBytes parseRtp 抛错分支（非 consumeSdp 异常，后者无裸抛路径）；241-243/258-261 已由 rtsp-client-edge.test.js 覆盖不重复造例（测试文件末尾登记）。
- 门禁：全仓 67220/68242（98.50%，+13）、lint 512 文件 0 警告、check 16/16、双契约审计 PASS。

## 第一百三十六波（gateway server-wsflv 残余分支补测）
- samples/gateway/src/server-wsflv.js 84.6→100%（104/104）：新增 samples/gateway/__tests__/server-wsflv-gaps.test.js +4 例（/healthz+404；initChunk/take 抛错→close(1011)——原型注入 FlvLoopSource.prototype 方法是唯一可达路径（纯函数无自然抛错源、socket.write 异步报错不同步抛）；conn error 事件→计时器清理——原生 TCP 握手后发 64bit 2^53 畸形帧触发 decoder "frame too large"）。
- 门禁：全仓 67252/68242（98.55%，+4）、lint 513 文件 0 警告、check 16/16、双契约审计 PASS。

## 第一百三十七波（gateway server-relay 残余分支补测 + 删除死代码 signalFrame）
- samples/gateway/src/server-relay.js 87.4→100%（186/186）：新增 samples/gateway/__tests__/server-relay-gaps.test.js +7 例（metaFromQuery 非法 JSON、OPTIONS 204、publish 流 error→500 与 writeHead 抛错吞噬、404 兜底、假订阅者 onDrop 统计、/status 升级 close(1008)、二进制上行转发）。删除死代码 signalFrame（零调用未导出，130 波先例）。
- 手法备注：假 req/res 直调 httpHandler 注入 error；server.channels 注入 send=()=>false 假订阅者迫使队列积压触发 onDrop；真 WebSocket 升级 /status 断言 1008。
- 门禁：全仓 67292/68234（98.62%，+7）、lint 514 文件 0 警告、check 16/16、双契约审计 PASS。

## 第一百三十八波（gateway server-rtsp 残余分支补测）
- samples/gateway/src/server-rtsp.js 88.9→100%（324/324）：新增 samples/gateway/__tests__/server-rtsp-gaps.test.js +4 例（/healthz+404、tick 早退兜底——原型注入 WsConnection.prototype.send 触发 terminate() 制造 alive=false 窗口、RTCP SR 信道 1 周期发送、455/501/PAUSE/文本入口/$ 块噪声）。122-124 为防御分支（run-to-completion 下不可观察，仅 terminate 真实存在），按 136 波注入先例覆盖。
- 门禁：全仓 67364/68234（98.72%，+4）、lint 515 文件 0 警告、check 16/16、双契约审计 PASS。

## 第一百三十九波（2026-09-20）
- mkv/src/source.js 92.31→**100%**（286/286），新增 mkv/__tests__/source-gaps.test.js +8 例：BAD_BLOB 守卫、HEAD 失败 GET Range:0-0 回退（200/206/双重失败）、TypeError→sequential 兜底与 rethrow、read 越界 OUT_OF_RANGE、openStream GET 失败、createByteSource BAD_SOURCE。无产品缺陷。
- 全仓 67408/68234（98.78%，+8）、lint 516 文件 0 警告、check 16/16、双契约审计 PASS。提交见 git log（wave 139）。
- 下一波候选：core/pipeline-mse.js 94.44%（527/558，defaultMseFactory/defaultSchedule/defaultRemuxerFactory 浏览器分支需甄别）→ rtmp/player.js 94.86% → webtorrent/index.js 95.20% → mov/demuxer.js 95.54%。

## 第一百四十波（2026-09-20）
- core/src/pipeline-mse.js 94.44→**99.64%**（556/558），新增 core/__tests__/core-pipeline-mse-gaps.test.js +9 例。要点：defaultSchedule timer 被 unref → 测试需 setInterval keep-alive；selectTrack abort 守卫（445-446/462-463）需 setImmediate 让 run() 先挂入 race，否则 destroy 先置 aborted 短路到 run() 的 420 守卫（lcov 不翻转的根因）。登记 31-32 不硬造（import 静态可解析不可达）。
- 全仓 67472/68234（98.88%，+9）、lint 517 文件、check 16/16、双契约审计 PASS。
- 下一波候选：rtmp/player.js 94.86% → webtorrent/index.js 95.20% → mov/demuxer.js 95.54% → webrtc/signaling.js 95.64% → samples/gateway slow-consumer.js 95.65%。

## 第一百四十一波（2026-09-20）
- rtmp/src/player.js 94.86→**99.68%**（310/311），新增 rtmp/__tests__/rtmp-player-gaps.test.js +5 例：setVideoTrack/setAudioTrack/addSample 抛错 → #fail（原型注入 Fmp4Remuxer.prototype + finally 还原）、audio track 正常路径、#onStable stableMs 复位退避。教训：断言须在 stop() 前（stop 置 STOPPED 掩盖 ERROR 态）；video 轨 track 事件即触发 #startFlushLoop。无产品缺陷。
- 全仓 67500/68234（98.92%，+5）、lint 518 文件、check 16/16、双契约审计 PASS。
- 下一波候选：webtorrent/index.js 95.20%（119/125）→ mov/demuxer.js 95.54% → webrtc/signaling.js 95.64% → samples/gateway slow-consumer.js 95.65%。

## 第一百四十二波（2026-09-20）
- webtorrent/src/index.js 95.20→**100%**（127/127），新增 webtorrent/__tests__/webtorrent-create-source-gaps.test.js +3 例。**修复产品缺陷**：index.js `new PlayerError(...)` 用了未导入标识符——`export { PlayerError } from ...` 是 re-export 不产生本地绑定，非法输入守卫触发即 ReferenceError；补 import 修复。教训：re-export ≠ 本地可用，补测前先实测守卫路径。
- 全仓 67518/68238（98.94%，+3）、lint 519 文件、check 16/16、双契约审计 PASS。
- 下一波候选：mov/demuxer.js 95.54%（107/112）→ webrtc/signaling.js 95.64% → samples/gateway slow-consumer.js 95.65% → mkv/demuxer.js 95.74% → flv/index.js 95.83% → core/limits.js 95.92%。

## 第一百四十三波（2026-09-20）
- mov/src/demuxer.js 95.54→**100%**（112/112），新增 mov/__tests__/mov-probe-gaps.test.js +2 例：顶层 wide atom hinted 分支（isom ftyp 置信 0.95>0.9 取 0.95）、Mp4Demuxer.probe 静态注入抛错 → 外层 catch null。无产品缺陷。
- 全仓 67530/68238（98.95%，+2）、lint 520 文件、check 16/16、双契约审计 PASS。
- 下一波候选：webrtc/signaling.js 95.64%（263/275）→ samples/gateway slow-consumer.js 95.65% → mkv/demuxer.js 95.74% → flv/index.js 95.83% → core/limits.js 95.92%。

## 第一百四十四波（2026-09-20）
- webrtc/src/signaling.js 95.64→**100%**（275/275），新增 webrtc/__tests__/signaling-gaps.test.js +5 例：defaultFetch 两分支（无 fetch notSupported / spy 委托）、Location 无法解析 fallback（'http://[' 宽松放行+严格抛错组合）、exchange 10s 超时（mock.timers——必须在 exchange 前 enable，apis 不支持 clearTimeout 单列）、sendCandidate 连接态发送。无产品缺陷。
- 全仓 67542/68238（98.99%，+5）、lint 521 文件、check 16/16、双契约审计 PASS。
- 下一波候选：samples/gateway slow-consumer.js 95.65%（88/92）→ mkv/demuxer.js 95.74%（1034/1080，46 行规模大可能需子代理）→ flv/index.js 95.83%（46/48）→ core/limits.js 95.92%（94/98）。

## 第一百四十五波（2026-09-20）
- core/src/limits.js 95.92→**100%** + flv/src/index.js 95.83→**100%**，新增 limits-gaps.test.js（+2）与 flv-index-probe-gaps.test.js（+1）。clampReadLength 为已导出契约面零内部调用 → 补测保留（不按未导出死代码先例删）。全仓破 99%。
- 全仓 67557/68238（**99.00%**，+3）、lint 523 文件、check 16/16、双契约审计 PASS。
- 下一波候选：mkv/demuxer.js 95.74%（1034/1080，46 行）→ samples/gateway slow-consumer.js 95.65%（88/92）→ hls/fmp4-muxer.js 96.04%（630/656）→ mp4/remuxer.js 96.07%（220/229）。

## 第一百四十六波（2026-09-20）
- samples/gateway/src/slow-consumer.js 95.65→**100%**（92/92），新增 slow-consumer-gaps.test.js +2 例（sendText 抛错 false、drain 抛错保留队列三阶段）。无产品缺陷。
- 全仓 67561/68238（99.02%，+2）、lint 524 文件、check 16/16、双契约审计 PASS。
- 下一波候选：mkv/demuxer.js 95.74%（1034/1080，46 行——规模大，可考虑子代理）→ hls/fmp4-muxer.js 96.04%（630/656）→ mp4/remuxer.js 96.07%（220/229）。

## 第一百四十七波（2026-09-20）
- mp4/remuxer.js 96.07→**100%**（229/229），新增 remuxer-gaps.test.js +3 例（sequenceNumber getter+resetSequence、metadata 轨 nmhd 占位、remuxDemuxer videoTrackId 过滤）。
- 用例侧关键点：track 必须带受支持 sampleEntryType（'avc1'）——buildStsd 对未知 entry 抛 unsupported sample entry，meta 轨唯一可达路径即配受支持 entry。方法名是 createMediaSegment（无 buildMediaSegment）。
- 残余 1 行登记不硬造：lcov DA:1 文件头注释伪影（c8 summary 已计 229/229）。
- 全仓 33792/34119（99.04%，+3）、lint 525 文件、check 16/16、双契约审计 PASS。
- 下一波候选：mkv/demuxer.js 95.74%（1034/1080，46 行——规模大，可考虑子代理）→ hls/fmp4-muxer.js 96.04%（630/656）。

## 第一百四十八波（2026-09-20）
- mkv/demuxer.js 95.74→**99.53%**（1075/1080），新增 demuxer-gaps.test.js +10 例（probe 三态+catch、构造/open/attach 守卫、Cues 非法 vint 吞错、短读源、未知长度 Tags/嵌套 Segment 边界探测、readSample abort 竞速+pendingResults 缓存、samplesInternal 守卫）。
- 关键勘误：0xFF 不是非法 vint（vintLength=1 全 1 数据位=未知长度元素），触发越界须 0x01 开头+截断。
- 登记 5 行不硬造：221-225 peekHeader 头部扩展循环（head 恒 min(16,剩余) 足额，readId VINT ≤8B ⇒ need≤16；不足额被 readExact 短读守卫前置拒绝）+ DA:1 注释伪影。
- 全仓 33833/34119（99.16%，+41）、lint 526 文件、check 16/16、双契约审计 PASS。
- 下一波候选：hls/fmp4-muxer.js 96.04%（630/656）→ 再扫 lcov 选最低。

## 第一百四十九波（2026-09-20）
- hls/fmp4-muxer.js 96.04→**99.24%**（654/659），新增 fmp4-muxer-gaps.test.js +4 例（空分片 null、safeCodec 降级告警、parseAot 两分支、toAvcc 往返+非法输入 PARSE_ERROR）。
- src 最小改动：既有 _internalForTest 出口补导出 safeCodec/parseAot/toAvcc。
- 登记 5 行不硬造：381-385 动态 import NOT_SUPPORTED 防御（仓库内真实模块不可 reject，同 140 波先例）+ DA:1 注释伪影。
- 全仓 33857/34122（99.22%，+24）、lint 527 文件、check 16/16、双契约审计 PASS。
- 下一波：再扫 lcov 选最低未达 99% 文件。

## 第一百五十波（2026-09-20）
- ape/src/index.js 96.07→**100%**（51/51），新增 index-gaps.test.js +1 例（probeApe null/undefined → TypeError 被 catch 吞为 null）。
- 全仓 33859/34122（99.22%，+2）、lint 528 文件、check 16/16、双契约审计 PASS。
- 下一波候选（按 lcov 排序）：mov/atom-compat.js 96.10%（247/257，10 行）→ samples/fixtures/net.js 96.15%（150/156）→ core/codec-string.js 96.15%（225/234）。

## 第一百五十一波（2026-09-20）
- mov/atom-compat.js 96.10→**99.22%**（255/257），新增 atom-compat-gaps.test.js +5 例（hasQuickTimeHints/detectCompressedMoov dcom/parseUdtaTags 外层+meta 内层四处 iterateBoxes 残字节与越界 size 防御 catch + TextDecoder 取证例）。
- 登记 2 行不硬造：233-234 TextDecoder 默认 fatal:false 永不抛（已取证固化）+ DA:1 注释伪影。
- 全仓 33867/34122（99.25%，+8）、lint 529 文件、check 16/16、双契约审计 PASS。
- 下一波候选：samples/fixtures/net.js 96.15%（150/156，6 行）→ core/codec-string.js 96.15%（225/234，9 行）→ flv/flv-parser.js 96.35%（264/274）。

## 第一百五十二波（2026-09-20）
- samples/fixtures/net.js 96.15→**100%**（156/156），既有 net.test.js 追加 +1 例（不传 nal 触发 defaultNal 101B IDR → FU-A 逐字节还原）。
- 全仓 33874/34122（99.27%，+7）、lint 529 文件、check 16/16、双契约审计 PASS。
- 下一波候选：core/codec-string.js 96.15%（225/234，9 行）→ flv/flv-parser.js 96.35%（264/274）→ mov/index.js 96.36%（53/55）。

## 第一百五十三波（2026-09-20）
- core/codec-string.js 96.15→**100%**（234/234），新增 codec-string-gaps.test.js +4 例（SPS null catch、00 00 03 emulation prevention、hevc 家族解析、isTypeSupported 抛错 catch）。
- 全仓 33882/34122（99.29%，+8）、lint 530 文件、check 16/16、双契约审计 PASS。
- 下一波候选：flv/flv-parser.js 96.35%（264/274，10 行）→ mov/index.js 96.36%（53/55）→ subtitle/track.js 96.46%（109/113）。

## 第一百五十四波（2026-09-20）
- flv/flv-parser.js 96.35→**100%**（274/274），新增 flv-parser-gaps.test.js +2 例（reset 全字段归零+可复用、frameType=5 Info/Command 0x57 静默忽略）。
- 全仓 33893/34122（99.32%，+11）、lint 531 文件、check 16/16、双契约审计 PASS。
- 下一波候选：mov/index.js 96.36%（53/55，2 行）→ subtitle/track.js 96.46%（109/113，4 行）→ core/mse-helper.js 96.46%（327/339，12 行）。

## 第一百五十五波（2026-09-20）
- mov/index.js 96.36→**100%**（55/55），新增 index-gaps.test.js +1 例（createDemuxer 假源 read reject → head catch → PROBE_FAILED）。
- 全仓 33894/34122（99.33%，+1）、lint 532 文件、check 16/16、双契约审计 PASS。
- 下一波候选：subtitle/track.js 96.46%（109/113，4 行）→ core/mse-helper.js 96.46%（327/339，12 行）→ webtorrent/source.js 96.77%（210/217，7 行）。

## 第一百五十六波（2026-09-20）
- subtitle/track.js 96.46→**100%**（113/113），新增 track-gaps.test.js +2 例（非法 encoding RangeError 回退 utf-8、probe(null) TypeError catch）。
- 全仓 33898/34122（99.34%，+4）、lint 533 文件、check 16/16、双契约审计 PASS。
- 下一波候选：core/mse-helper.js 96.46%（327/339，12 行）→ webtorrent/source.js 96.77%（210/217，7 行）→ webrtc/mock-signaling.js 97.05%（99/102）。

## 第一百五十七波（2026-09-20）
- core/mse-helper.js 96.46→**100%**（339/339），新增 core-mse-helper-gaps.test.js +4 例（abortThenClear 三态、append 正常+未知 key、drainAll、destroy endOfStream 竞态吞错）。
- 用例侧：FakeMediaSource 必须带实例 isTypeSupported→true 才能过 addTrack。
- 全仓 33910/34122（99.37%，+12）、lint 534 文件、check 16/16、双契约审计 PASS。
- 下一波候选：webtorrent/source.js 96.77%（210/217，7 行）→ webrtc/mock-signaling.js 97.05%（99/102，3 行）→ ts/index.js 97.10%（67/69，2 行）。

## 第一百五十八波（2026-09-20）
- webtorrent/source.js 96.77→**100%**（217/217），新增 source-gaps.test.js +1 例（withSizeAlias getter 别名）。
- src 最小改动：withSizeAlias 加 export（重构遗留私有 helper，类自身 96 行已有 byteLength；导出直测，未删除）。
- 全仓 33917/34122（99.39%，+7）、lint 535 文件、check 16/16、双契约审计 PASS。
- 下一波候选：webrtc/mock-signaling.js 97.05%（99/102，3 行）→ ts/index.js 97.10%（67/69，2 行）→ core/pipeline-webcodecs.js 97.10%（670/690，20 行）。

## 第一百五十九波（2026-09-20）
- webrtc/mock-signaling.js 97.05→**100%**（102/102），新增 mock-signaling-gaps.test.js +2 例（单通道/回环对 drainRemoteCandidates、回环 sendCandidate 出线）。
- 全仓 33920/34122（99.40%，+3）、lint 536 文件、check 16/16、双契约审计 PASS。
- 下一波候选：ts/index.js 97.10%（67/69，2 行）→ core/pipeline-webcodecs.js 97.10%（670/690，20 行）。

## 第一百六十波（2026-09-20）
- ts/src/index.js 97.10→**100%**（69/69），新增 index-gaps.test.js +1 例（静态注入 TsDemuxer.probe 抛错 → 出口 catch 返 null，finally 还原）。
- 注意：ts ProbeResult 字段是 container 不是 format；TsDemuxer.probe 本体对 null/{}/42 等全返 null（内部 catch 已登记不可达）。
- 全仓 33922/34122（99.41%，+2）、lint 537 文件、check 16/16、双契约审计 PASS。
- 下一波候选：core/pipeline-webcodecs.js 97.10%（670/690，20 行，规模大可派子代理）。

## 第一百六十一波（2026-09-20）
- core/pipeline-webcodecs.js 97.10→**99.71%**（688/690），新增 core-webcodecs-pipeline-gaps.test.js +6 例（defaultSchedule 真实定时器、外部钟 resync break、无 renderer _render、双重 close、视频切轨中途 destroy、音频复用路径 getter destroy）。
- 手法坑：工厂内 push 先于外部 configure，区分「新轨解码器」要用闭包标志（created.length>=1 在工厂体内判断），不能在 configure 里判 length===1（首解码器 configure 时已为 1，误杀 init 致 558 抛错）。
- 登记 226-227 不可达：225 条件仅新输出路径可真，而 217→223 同步无 await 不可翻转。
- 全仓 33940/34122（99.46%，+18）、lint 538 文件、check 16/16、双契约审计 PASS。
- 下一波：重新跑 c8 排序选最低 pct<99.5 文件。

## 第一百六十二波（2026-09-20）
- mp4/webcodecs-pipeline.js 97.19→**100%**（214/214），新增 webcodecs-pipeline-gaps.test.js +2 例（双不支持 DECODE_ERROR、音频 output/error 回调）。
- 用例侧：stats mock 必须带 markDemuxed，否则 pump 成功路径抛错被 decode catch 记成假错误。
- 全仓 33946/34122（99.48%，+6）、lint 539 文件、check 16/16、双契约审计 PASS。
- 下一波候选：rtsp/source.js 97.22%（105/108，3 行）→ hls/mse-controller.js 97.31%（290/298，8 行）→ core/stats.js 97.39%（112/115，3 行）。

## 第一百六十三波（2026-09-20）
- rtsp/src/source.js 97.22→**100%**（108/108），新增 source-gaps.test.js +2 例（close 非重连派 end、start 前 client error 清 timer 并 reject）。
- 全仓 33949/34122（99.49%，+3）、lint 540 文件、check 16/16、双契约审计 PASS。
- 下一波候选：hls/mse-controller.js 97.31%（290/298，8 行）→ core/stats.js 97.39%（112/115，3 行）→ hls/utils.js 97.61%（205/210）。

## 第一百六十四波（2026-09-20）
- hls/src/mse-controller.js 97.31→**100%**（298/298），新增 mse-controller-gaps.test.js +3 例（Quota 回调异常吞掉、remove error、destroy removeSourceBuffer/endOfStream 竞态）。
- FakeMediaSource 需构造微任务派发 sourceopen；初版 pending 已修正。
- 全仓 33957/34122（99.51%，+8）、lint 541 文件、check 16/16、双契约审计 PASS。
- 下一波候选：core/stats.js 97.39%（112/115，3 行）→ hls/utils.js 97.61%（205/210）。

## 第一百九十六波（2026-09-20）
- CMAF 截断视频配置防御：修复 avcC/hvcC 等配置盒声明长度超过 moov 时返回截短 description 的问题，改为返回 null，并新增 avcC 回归测试。
- 全仓 2934/2934、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百九十五波（2026-09-20）
- CMAF 截断 ASC 防御：修复 DSI 声明长度超过剩余数据时返回截短 AudioSpecificConfig 的问题，改为返回 null，并新增回归测试。
- 全仓 2933/2933、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百九十四波（2026-09-20）
- MP4 顶层 mdat size-zero 回归：将渐进式 fixture 的 mdat 大小字段改为 0，验证按文件末尾计算盒边界并继续完整解封装 8 个样本。
- 全仓 2932/2932、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百九十三波（2026-09-20）
- MKV 无限长度源尾部半截元素头回归：使用 size=Infinity 的流式数据源追加截断 Void 元素头，验证 open() 将尾部残缺按流尾处理并保持媒体信息可用。
- 全仓 2931/2931、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百九十二波（2026-09-20）
- WebCodecs 延迟音频输出清理回归：在音频输出工厂等待期间 destroy，验证迟到的输出实例仍被销毁回收，覆盖生命周期代数与异步清理路径。
- 全仓 2930/2930、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百九十一波（2026-09-20）
- RTMP 重连构造失败回归：注入重连 WebSocket 构造异常，验证播放器进入 error，定时器连接失败 Promise 被消费，不产生未处理拒绝。
- 全仓 2929/2929、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百九十波（2026-09-20）
- MP4 空源打开回归：验证空 MemoryDataSource 调用 Mp4Demuxer.open() 明确拒绝并返回 PARSE_ERROR，不静默完成。
- 全仓 2928/2928、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百八十九波（2026-09-20）
- 字幕空文本布局回归：验证活跃空文本 cue 仍生成稳定空行、零宽度与默认字号，覆盖无文本片段时的字号回退路径。
- 全仓 2927/2927、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百八十八波（2026-09-20）
- ASS 样式未知行回归：验证 [V4+ Styles] 中未知行被忽略，不生成伪样式，也不增加畸形块计数。
- 全仓 2926/2926、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百八十七波（2026-09-20）
- CMAF 缺失 ASC 回归：构造包含 esds 但没有合法 DSI 的初始化字节，验证 findAudioSpecificConfig() 返回 null 而不误报配置。
- 全仓 2925/2925、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百八十六波（2026-09-20）
- AMF0 不支持类型回归：验证公开 encodeAmf0(Symbol()) 明确抛出带类型信息的 TypeError，覆盖编码器保护路径。
- 全仓 2924/2924、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百八十五波（2026-09-20）
- TS 引擎单包异常隔离回归：注入包解析异常，验证每个异常转为 error 事件、后续包继续消费，flush 仍可完成。
- 全仓 2923/2923、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百八十四波（2026-09-20）
- AAC LATM 截断扩展 AOT 回归：构造不完整扩展 AOT 候选，验证有界 ASC 扫描继续工作并保持输出结构合法。
- 全仓 2922/2922、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百八十三波（2026-09-20）
- H.264 AnnexB 单帧 fixture 回归：实际调用 makeFrameAnnexb()，验证 4 字节 AnnexB 起始码、IDR NAL 类型与完整 I_PCM 帧长度，替代此前仅 void 引用的覆盖空洞。
- 全仓 2921/2921、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百八十二波（2026-09-20）
- WAV 非法 PCM 组合回归：验证 convertToFloat32Planar() 对未支持的 formatTag/bitsPerSample 组合抛出 NOT_SUPPORTED，并保留格式参数。
- 全仓 2920/2920、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百八十一波（2026-09-20）
- 字幕 Canvas rAF 时钟驱动回归：注入 requestAnimationFrame/cancelAnimationFrame，覆盖函数时钟、currentTimeUs 对象时钟、视频 currentTime 三条路径，并确认 stop 取消调度、回调执行实际绘制。
- 全仓 2919/2919、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百八十波（2026-09-20）
- MPEG-TS fixture 零长度自适应字段回归：以 auPadBytes=302 构造非 PCR 尾包仅剩 1 字节空隙，验证 adaptation_field_length=0、183 字节载荷及 AF+payload 控制位。
- 全仓 2918/2918、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百七十九波（2026-09-20）
- A/V 同步控制器播放速率回归：验证 AvSyncController.setRate() 委托内部时钟，接入外部 master clock 后 start() 不重置内部锚点，且主钟读取优先外部源。
- 全仓 2917/2917、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百七十八波（2026-09-20）
- CMAF sidx 64 位索引回归：构造 version=1 sidx，验证 earliestPresentationTime 与 firstOffset 的 64 位解析，以及 SAP 引用表字段保持正确。
- 全仓 2916/2916、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百七十七波（2026-09-20）
- WebTorrent infohash 异常回退回归：注入 WebCrypto subtle.digest() 抛错，确认 computeInfoHash() 安全返回 null，不泄漏底层异常。
- 全仓 2915/2915、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百七十六波（2026-09-20）
- HTTP Range 忽略响应的取消异常防御回归：服务器对非零 offset 忽略 Range 返回 200 且 body.cancel() 自身抛错时，确认原始 SOURCE_ERROR 不被取消异常覆盖，覆盖 http-range-source.js 行 154–156。
- 全仓 2914/2914、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百七十五波（2026-09-20）
- RTMP FLV 畸形 script Tag 错误归一回归：非法 AMF marker 触发 script 解析异常时统一派发 PARSE_ERROR，后续未知 Tag 仍可消费且样本计数不污染；覆盖 flv-demuxer.js 行 120–121。
- 全仓 2913/2913、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百七十四波（2026-09-20）
- ASS 标签状态机颜色与缩放边界回归：验证 \2c 支持但不改描边、\3c 更新描边色、\fscy 小数纵向缩放，补齐 tags.js 行 100–102、140–141 分支。
- 全仓 2912/2912、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百七十三波（2026-09-20）
- TS PES stream_id 分类边界回归：覆盖视频 e0–ef、音频 c0–df 两端边界及音视频互斥判断，补齐 isAudioStreamId() 未执行路径；ts/src/pes.js 定向行/分支/函数覆盖率 100%。
- 全仓 2911/2911、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百七十二波（2026-09-20）
- Gateway 聚合入口 close 生命周期回归：独立启动 ws-flv、rtsp-ws 与 channel relay，等待全部 ready 后验证 startGateways().close() 无异常，随后 dispose() 可安全收尾；samples/gateway/src/index.js 行/函数覆盖率 100%。
- 全仓 2910/2910、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百七十一波（2026-09-20）
- VideoFrameRenderer 2D 初始化异常回归：Canvas2D getContext() 抛错时安全归一为 NOT_SUPPORTED，并验证全局 document 清理；renderer 定向行/分支/函数覆盖率 100%。
- 全仓 2908/2908、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百七十波（2026-09-20）
- FLV track waiter 清理回归：直接验证 waitForTracksOrEos() 在 video 信号完成后 resolve，并清理 video/audio/complete 全部监听，固化避免事件监听泄漏的行为。
- 全仓 2907/2907、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百六十九波（2026-09-20）
- Stats reset 全状态回归：断言 reset 同时清零全部计数器、decode 平均/最大耗时、FPS EMA 与渲染时间锚点；core/src/stats.js 定向行/分支/函数覆盖率 100%。
- 全仓 2905/2905、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百六十八波（2026-09-20）
- MP4 probe 防御分支补测：hostile `byteLength` getter 抛错时，Mp4Demuxer.probe() 同步返回 null 而不抛异常；与 ftyp/wide/styp/mdat 品牌矩阵共同验证。
- 全仓 2905/2905、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百六十七波（2026-09-20）
- HLS utils 边界补测：无效 URL/base、空/畸形属性、奇数十六进制、MIME 组装、SN/时间续播边界、EWMA 无效样本与暖机默认估计。
- hls/src/utils.js 定向行/函数覆盖率 100%。全仓 2904/2904、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百六十六波（2026-09-20）
- 补测 core AudioWorklet destroy 异常分支：node/gain disconnect 抛错时仍完成 context/节点清理、destroy 事件与幂等；销毁后 resume/clearBuffer 安全返回。
- audio-worklet-player.js 定向覆盖率达到 100%（行/分支/函数）。
- 全仓 2902/2902、lint 541 文件、check 16/16、结构层与运行时契约审计 PASS。

## 第一百六十五波（2026-09-20）
- 真实 Chrome 探针复现 MP4 双轨 MSE 初始化缺陷：视频 SourceBuffer 追加 init 后再创建音频 SourceBuffer，Chrome 抛 QuotaExceededError(22)。
- 修复 mp4/demo/index.html：先注册所有 video/audio SourceBuffer，再统一追加 init segment；修正 scripts/e2e/demo-smoke.mjs 的 MP4 按钮选择器 #playUrl→#loadUrl。
- core、MP4、HLS 真机 smoke 全通过；MP4 readyState=4 且 currentTime 持续推进；全仓 2901/2901、lint 541 文件、check 16/16、双契约审计 PASS。
