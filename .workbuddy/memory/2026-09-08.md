# 2026-09-08 工作日志（纯前端播放器）

- 第三十九波：第二轮 I2 续——轨道选择与解码链重建。player.js 新增 selectedTracks{video,audio,text}（load 后按 default→首轨初始化），_pump 改为只拉 _activeTrackIds() 而非全部非 metadata 轨；selectTrack 补完：类型/存在性校验（STATE_ERROR）、同轨幂等、_pumpToken++ 中断并重启泵、广播 trackchange、管线异常包装 PlayerError。WebCodecs 管线加 active 选中表，非选中轨样本丢弃，selectTrack 改 async：视频 close 旧解码器并按新 codec 重建，音频 close+clearBuffer+重建且同采样率/声道复用音频输出。MSE 管线同样加 active，init/seek 只为选中轨建 SourceBuffer，selectTrack 为新轨 addTrack+补写 init 并丢弃旧轨待封装样本。新增 select-track.test.js 5 例；core 定向 26/26 全绿，全仓 114 文件 **955/955 全绿**。台账记入 §39。
- 第三十八波：第二轮 I2 续——新增 core/src/pipeline-mse.js（MsePipeline + msePipelineFactory），样本→fMP4 重封装（默认延迟 import mp4/Fmp4Remuxer）→MseHelper 串行 appendBuffer→mediaElement 内建解码；GOP 边界+目标时长成段、bufferedAhead 背压、end() flush 后 endOfStream、seek 逐轨 resetTrack(false) 并对齐元素时间轴；player.js 在 mse 路线默认挂该工厂，样本泵结束调用 pipeline.end?.()。新增 pipeline-mse.test.js 9 例，管线+Player 定向 21/21 全绿。**G9 复核纠偏**：mp4 remuxer:122/124、box-builder:288、mp4 webcodecs-pipeline:20/35/98、core mse-helper:28/112 此前只加了十码 import 未替换 throw，本波全部收口为 stateError/notSupported/decodeError。台账已记入 §38，core/README 补 MSE 管线说明。全仓串行 113 文件 **950/950 全绿**（941+9）。注意：mp4/__tests__/webcodecs-pipeline.test.js 断言 buildVideoConfig 误用抛 TypeError，故「非视频轨」类型断言保留 TypeError，仅「缺 codec 串」收口为 stateError。
- 第三十七波：第二轮 I2 续——新增 core/src/pipeline-webcodecs.js（WebCodecsPipeline + webcodecsPipelineFactory + audioDataToPlanar），串起 VideoDecoder/AudioDecoder→f32-planar→AudioWorklet、AvSync render/drop/wait、渲染器 close-in-finally、字幕 cue、seek 清缓冲与时间偏移、destroy 释放；player.js 在 webcodecs 路线默认挂该工厂并转发 firstframe/stall/underrun/cue 事件，currentTimeUs 优先取管线主时钟。新增 pipeline-webcodecs.test.js 8 例，管线+Player 定向 12/12 全绿；MSE 管线工厂仍未接入，保留为下一波候选。台账已记入 §37，core/README 补“播放编排与管线”章节。
- 第四十波：第二轮 I2 续——起播缓冲、背压与统计/进度闭环。player.js 新增 `play()→_prebuffer(token)` 起播前向缓冲（按媒体时长跨度或管线 bufferedAheadUs 灌到 bufferTargetUs 默认 3s、直播 liveLatencyUs；退出条件：水位达标/EOS/prebufferMaxSamples 兜底/token 抢占），常规泵每次推帧后 `_backpressure(token)`（超 bufferTargetUs×backpressureFactor 默认 2 暂停拉流、轮询回落），预缓冲与背压均发 `'buffering'` 事件；`buffered` 三级回落（demuxer.getBufferedRanges→管线 getBufferedRanges→bufferedAheadUs 合成→空），新增 `bufferedAheadUs` getter；`stats.bitrateBps` 按滑动窗口统计不再恒 0；`load()` 透传 demuxer `'progress'`；统一 `_deliver()` 收口样本投递。两条管线补 `bufferedAheadUs`/`getBufferedRanges()`（WebCodecs 取音频输出水位优先、MSE 取活动轨 SourceBuffer 最小水位）。新增 buffering.test.js 8 例，core 17 文件全绿。台账 §40。**现盘发现**：`Demuxer.destroy()` 在异步生成器被永不 resolve 的 await 卡住时会挂起（V8 行为），Player 侧 pumpToken 已停泵但 Demuxer 销毁受约束；已如实收编为路线图限制，不擅自扩大修改。
- 第四十一波：第二轮 I2 续——`Demuxer.destroy()` 实时性工程化缓解。`core/src/demuxer.js` 的 destroy 不再 `await gen.return()`（V8 异步生成器在 `await` 永不 resolve 的 promise 时 `return()` 需等其落地），改为标记各迭代器 done 并清空映射后立即返回；in-flight readSample 落地后的样本由 Player 按 state==destroyed 丢弃，外部 readSample 由 `_requireUsable` 拒绝。新增 demuxer-destroy.test.js 3 例（迭代器阻塞 1s 内完成、幂等、Player.destroy 1.5s 内退出）。core 18 文件全绿。台账 §41。README 已知限制条目由「实时限制」改为「不再挂起」现状说明；后续可考虑为 readSample 引入 AbortSignal/超时。
- 第四十二波：第二轮 I2 续——直播落后丢帧追赶 + AvSync resync 语义收口。**现盘纠偏**：pipeline `_pumpFrames` 只处理 render/drop/wait，resync 落到「渲染」，大幅迟到帧（如直播落后 8s）被渲染而非丢弃，违反 §5「迟到丢帧」（由 live 追赶测试暴露：追赶触发后 0 帧被丢）。修复：`pushSample` 累计 `_liveEdgeUs`；`_maybeLiveCatchUp()` live+liveLatencyUs 时落后超阈值（默认 liveLatency/2、下限 500ms）主钟重锚到 liveEdge−liveLatencyUs，旧帧逐个丢，广播 'catchup'；新增 `_masterRealignToUs`（音频可清缓冲归零+偏移锚定；外部不可控钟不重锚返回 false）；resync 显式区分方向（drift<0 丢帧 / drift>0 重锚超前帧），seek/视频切轨重置 liveEdge。player 转发名单补 'catchup'。新增 live-catchup.test.js 4 例，受影响 6 模块 39/39 全绿。台账 §42。
- 第四十三波：第二轮 M3——**真实浏览器端到端验收（WebCodecs 主路线打通）**。基建：系统 Chrome 152 + playwright-core（免下载 Chromium）+ serve.mjs；harness scripts/e2e/player-harness.html（15 步自断言）+ 驱动 run.mjs；素材 samples/e2e/bbb480_30s.ts（mux.dev BBB 480p TS，annexb）+ sintel-trailer.mp4（w3.org 52s seekable）。**端到端暴露 6 个真实缺陷并修复**（Node 假实现测不出）：①hasAudioWorklet 直读 prototype.audioWorklet accessor → Chrome Illegal invocation，改 `in` 探测；②能力探测默认 codec 清单不含实际内容 codec（avc1.64001F）→ route 误判 none，capabilities 导出 DEFAULT_* + Player detectForMedia 并入实际 codec；③音频探测缺 sampleRate/channels → Chrome 判不支持，补 48k/stereo；④TS annexb 样本直喂 VideoDecoder（期望 avcC）→ 解码错误自动 closed，decode 前 annexbToAvcc；⑤pipeline.seek reset() 后未重 configure → decode on unconfigured，存 _videoConfig/_audioConfig 并在 seek 重配；⑥registry 恒空（宿主注册协议零调用，接线缝隙），harness 按 §10 注册打通。验收：TS 全 PASS、MP4 **15/15 PASS**（seek 20s→16.5s 关键帧对齐+恢复、fps≈25、buffered 6.3s、destroy 干净，VD-CLOSE 探针确认仅 destroy）。新增防回归 webcodecs-e2e-regress.test.js 3 例。core 19 文件 109 例全绿。台账 §43。MSE 真实浏览器端到端留待后续波。
- 第四十四波：第二轮 M3 续——**MSE 路线真实浏览器端到端验收（兜底路线打通）**。新增 scripts/e2e/mse-harness.html（19 步自断言，强制 routePreference:['mse']）+ 诊断页 mse-diag.html（init 逐段 append 探测 + SourceBuffer 配额探测）+ run.mjs `--harness=` 参数。MP4 素材 19/19 PASS（2×SB + 2 段 init → 元素真实解码 854x480 / readyState=4 / 首帧 219ms → 时间轴推进 → Player 时钟跟随元素 → 渲染 23 帧 0 丢 → seek 20s→16.5s 关键帧对齐后恢复 → 25 段/3009 样本/4.88MB → destroy 干净）；WebCodecs 复跑 15/15 无回退。**端到端暴露并修复 5 个真实缺陷**：①chooseRoute 完全忽略宿主 routePreference（Player 有选项从未参与裁决）→ 改 chooseRoute(caps,info,{preference}) 按偏好顺序取首个可用；②MseHelper.addTrack 用**实例**调 isTypeSupported（实为构造器静态方法，实例上没有）→ 全轨误判不支持、load 直接 NOT_SUPPORTED，新增 isTypeSupported() 走构造静态/全局（含 ManagedMediaSource），mseIsTypeSupported 同步增强；③**tkhd 漏 layer(2B)**（parseTkhd 少读、buildTkhd 少写，自产自解"自洽"但不符 ISO）→ alternateGroup/volume/矩阵整体错位 2B、width/height 恒 0 → fMP4 init segment avc1 宽高 0 → **Chrome 拒收 init segment，MSE 整体不可用**（顺带补齐 WebCodecs 的 codedWidth/Height），修后真实 MP4 解析出 854x480；④MsePipeline.init 逐轨「建 SB→写 init」交错 → Chrome 在某 SB 写过数据后拒绝新建 SB（QuotaExceededError，有头/无头均复现，干净 MS 上连建 2 个正常）→ 改两阶段「先建齐 SB 再统一 append」，诊断页验证有画面；⑤canMse 在已深探测时重复实时判定（Node 无 MediaSource 恒 false）→ 深探测 mimeTypes 非空直接采信。新增 core/__tests__/mse-e2e-regress.test.js（5 例）+ mp4/__tests__/tkhd-layout.test.js（4 例，含 layer≠0 错位探针与 builder/parser 往返）；tkhd +2B 致 fixture 需 `npm run fixtures` 重建（重建后 artifacts-edge 21/21）。全仓 **982/982 全绿**（973→982）。台账 §44。遗留：TS 不在 REMUXABLE 白名单（mp4/mov/flv），MSE 验收以 MP4 覆盖。

- 第四十五波：第二轮 I5 安全项全仓现盘审计与收口。新增 `core/src/url-guard.js`（协议白名单：fetch 仅 http/https、ws 仅 ws/wss、import 仅 http/https；assertSafeUrl 等抛封闭错误码）+ `core/src/limits.js`（`DEFAULT_MAX_READ_BYTES=64MB`/`MAX_SAMPLE_BYTES=32MB`/`MAX_MOOV_BYTES=64MB`/`MAX_SMALL_RESOURCE_BYTES=1MB`/`MAX_SCAN_BYTES=256MB` + `assertByteLength` + `BoundedMapCache` FIFO 有界缓存）；接入 HttpRangeDataSource（构造协议校验+读取上界）、hls segment-loader/decrypter（密钥 URI 白名单+响应体 1MB 上界+密钥缓存 FIFO）、webrtc/rtmp/rtsp signaling（schema 校验+原型污染键拒绝+地址白名单）、webtorrent loader（import URL 白名单）、mp4/mov/flac demuxer（readCapped 透传字节上界）；hls parseAttributes/ASS 结构识别加正则灾难性回溯防护。现盘 ③postMessage/MessageChannel 为 N/A（site/demo 无跨窗口通信）。新增防回归 core/__tests__/security-i5.test.js（27 例）+ mp4/__tests__/security-limits.test.js（11 例，含 readCapped）；hls aes128 测试密钥 URI 迁合法 https。**I5 六类全 ✅，受影响模块定向回归无回退，安全测试 27/27 + 11/11 全绿**，全仓基线待补跑复核。台账 §45。

- 第四十六波：第二轮 I4 README 与实现一致性审查与收口。现盘 16 模块 README 六要素结构齐全（core 原缺独立「快速开始」章节，已补）；core 新增「安全与输入防护（评审 I5）」章节文档化 url-guard/limits 导出面（core/src/index.js:70-90）；mp4 API 说明表补 Mp4WebCodecsPipeline（mp4/src/index.js:14 已导出、快速开始已用但未列）；**核实并修正字段名**：MsePipeline 读 `options.mediaElement ?? options.video`（pipeline-mse.js:59）、WebCodecsPipeline 读 `options.canvas`（pipeline-webcodecs.js:522），core 快速开始示例原写 `videoElement` 误，改 `mediaElement`；已知限制抽查如实（hls AES-128 已支持声明与 v0.2 定稿一致、flv 定位声明一致）。本波纯文档修正，无代码改动，不影响全仓 982 基线。台账 §46。

- 第四十七波：第二轮 I6 site 汇总页导航一致性审查。汇总页实际为 `site/demo/index.html`（`site/index.html` 仅跳转）；模块卡片由 `site/nav.js` 的 MODULES 动态生成（`a.href='../../'+m.href`）。现盘确认 MODULES 含全部 16 业务模块（core/mp4/mov/mkv/webtorrent/ts/flv/hls/cmaf/wav/flac/ape/subtitle/webrtc/rtsp/rtmp），href 与各模块 `demo/index.html` 实际路径一一对应、拼接后相对路径正确，页签与路径无错位。顶部 tab 为精选快捷入口（不全覆盖），全量由卡片网格承担，不冲突。**I6 通过，无需修正**。I 系列仅剩 I3（demo 浏览器可用性验收）待推进。
- 第四十八波：第二轮 I3 demo 可用性真机验收收口（I 系列最后一项）。基建 scripts/e2e/demo-audit.mjs（16 demo 四维自动验收：L1 无未捕获异常 / L2 交互注入拖放·file·URL·按钮 / L3 降级提示 / L4 skin 资源 404）+ mk-hls-fmp4.mjs（ts demux + Fmp4Remuxer 再生 fMP4 直通 HLS 素材 samples/e2e/fmp4/）。暴露并修复 8 缺陷：①wav/flac/ape skin 资源 404（旧皮肤系引 ../css|js|assets 模块内副本，实际收敛到 site/，改指 ../../site/*）②webtorrent demo clearErr 未定义 ③mp4 demo durationSec→durationUs ④mp4/mov demo remuxDemuxer 旧 for-await 用法→await 数组 ⑤flac demo computePeaks/drawWaveform 错从自身导入（实属 wav 模块）⑥hls SegmentLoader webidl fetch 未 bind(globalThis)→Chrome Illegal invocation，HLS 真机拉流全挂（src 级）⑦hls MseController removed-SourceBuffer 读 buffered 连环 pageerror（onErr/getBuffered 加防御）⑧hls fmp4-muxer init 移除 stts/stsc/stsz/stco 四空表对齐 mp4 remuxer。结果 16/16 全绿、site 404=0、mp4/mov readyState=4、ts 29.98s、hls fMP4 11 分片全拉。遗留：hls TS→fMP4 transmux 产物 Chrome 151+ MSE 真机被拒（音频轨可 append、二分排除 avc1/stbl 空表，根因未收敛，后续专项）。全仓回归 1009/1009。台账 §48、checklist ✅。I 系列（I2–I6）全部收口。
- 第四十九波：HLS TS→fMP4 transmux Chrome 151+ MSE 真机拒收根因专项（I3 遗留消除）。真机二分基建 scripts/e2e/{transmux-diag,hls-ts-e2e,dump-init,dump-media}.mjs（落盘+fetch 路径规避 evaluate 序列化歧义）。根因锁定 hls/src/fmp4-muxer.js 两处：①videoSampleEntry 的 VisualSampleEntry 头部漏写 pre_defined[3]（标准 16 字节，旧实现只 8 字节，avc1 错位致 init 被拒）②remux() 内 video 样本 size 取 ts demux 返回的 annexb 原始帧长，但 data 存 avcc（更短），Σ trun size≠mdat 致 media 被拒。修复：补 pre_defined[3] 三 u32；size 无条件取 avcc.byteLength。验证：transmux-diag 7 变体（A styp+整段/B GOP/C 去styp/D GOP去styp/G mp4init+GOP/H mp4init+整段/I 真实remux路径）全 initOk+mediaOk 过；hls-ts-e2e 端到端 hls demo 加载单分片 TS 形态 HLS 真机 readyState=4 848×480 pageError=0；ts-remux.test.js 补 2 防回归断言（stsd 对齐 mp4 标准、Σ size==mdat）9/9；全仓 fail=0。基建 samples/e2e/ts-hls/{playlist.m3u8,seg-000.ts}+docs/review/i3/hls-ts-transmux.png。I 系列（I2–I6）+ transmux 遗留全部收口。
