# 2026-09-15

- 修复 MKV `MkvDemuxer.#parseCuesAt()` 对 EBML 子元素顺序的隐式依赖：先收集 `CueTrackPositions`，完成 `CueTime` 解析后再统一生成 cues，因此 CTP 先于 CueTime 时不再丢失索引。
- 新增 `mkv/__tests__/mkv-cues-extra.test.js`，覆盖多轨、locate、未知字段、额外字段、反序元素及 1 字节 VINT，共 6 例；MKV 全量 185/185、全仓 2426/2426 通过。
- 全仓 lint 479 个 JS 文件通过；`npm run check` 串行重跑通过，16/16 模块达标（报告合计 2355/565）；`git diff --check` 通过。
- 提交 `bcb15b8 fix: parse MKV cues independent of element order`，暂未推送远端。
