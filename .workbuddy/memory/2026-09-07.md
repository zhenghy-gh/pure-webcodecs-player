# 2026-09-07

## round-1 评审第四波：现盘扫描 + hls/ts/mkv 修复

- 起点：仓库最后改动停在 2026-08-26（subtitle 回执）。先跑基线 `node --test` 递归 **860/860 全绿**，
  再逐条 grep 实码核对 `docs/review/round-1-问题清单.md`，发现大量条目已修但台账未勾销。
- 已闭环补登记（磁盘证据）：core#1/#2/#3/#6/#7/#10、mkv#1/#2/#4、hls#1/#2/#4/#6/#7、
  webtorrent#1/#3/#4、subtitle#1/#2。
- 本轮实际修复：
  1. **hls#3 残余**：`player.js:_startPipeline()` 无参且内部 `nextSegmentIdx=0`，抹掉
     `_loadMediaPlaylist` 算出的 resumeIdx → 改为 `_startPipeline(startIdx=0)`，
     `_lastAppendedSn` 仅首次装载清空。
  2. **hls#5 错误体系**：LoadError 旧签名 `(message,info)` → `(code,message,info)`
     （segment-loader:89、player:125）；level-controller RangeError → stateError；
     mse-controller 七处裸 Error → PlayerError 十码，并接通 `_onQuotaEvict` 死回调、
     `isTypeSupported` 改走保存的 MSCtor；data-source / aes-cbc / fmp4-muxer 同步收口。
  3. **ts#4 残余**：新增 `parseHevcSpsConfig(sps)`，buildHvcc 的 chroma/bitDepth/nesting
     由硬编码改从 SPS 解析（main10、4:2:2 不再坏）；fixture `hevcSps` 支持
     chromaFormatIdc/bitDepth/temporalIdNesting 参数。
  4. **mkv#3 加固**：`parseBlockHeader` 裸 Error → `PlayerError('PARSE_ERROR')`。
- 测试：新增 7 例（hls 6 + ts 1），全仓 **867/867 全绿**；`scripts/lint.mjs` 317 文件通过。
- 台账新增第 9 章「第四批复核记录」，含已闭环表、修复说明、仍开放清单（core#4/#5/#9、S2 公共件、P2 建议）。
- 回执：`hls/review-round1-hls-receipt.md`。

## 项目约定（复用）

- 评审流程：owner 修 → 写 `review-round1-<模块>-receipt.md` 回执 → reviewer 磁盘实码复核 →
  在 `docs/review/round-1-问题清单.md` 追加复核记录勾销。行号会漂移，以问题实质认定。
- 契约硬约束：错误体系只能用 core 十码（PROBE_FAILED/PARSE_ERROR/NOT_SUPPORTED/SOURCE_ERROR/
  NETWORK_ERROR/DECODE_ERROR/SEEK_UNSUPPORTED/TIMEOUT/ABORTED/STATE_ERROR），全仓零裸 Error；
  时间基对外一律整数 µs；Node/浏览器双环境不得裸引用 window/MediaSource。

## round-1 评审第五波：现盘扫描 + core 三处建议项修复

- **关键发现**：第四批（§9.3）「仍开放」清单已严重过期——core#4(codec-string 四 API)、core#5
  (capabilities 三 API+真缓存)、core#9(worklet 主时钟名/us/underrun)、S2(logger/HttpRangeSource/
  ChunkBuffer) 以及 hls fmp4 编造 `.42E01E`、toAvcc 透传坏流、纯音频强建视频轨、QuotaExceeded 接线
  **全部已在磁盘落地**。逐条 grep 实码确认后于台账 §10.1 补勾销。
- 本轮实际修复（core 建议项，均有实证复现）：
  1. **readUEG 前导零溢出**：`exp-golomb.js:38` `(1 << leadingZeros)` 在 leadingZeros=31 时
     `(1<<31)=-2147483648` 溢出 → 改 `2 ** leadingZeros`（精确无符号溢出）。
  2. **byte-stream isView 丢弃子窗口**：`byte-stream.js:28-30` ArrayBuffer.isView 分支忽略
     byteOffset/byteLength → 改与 Uint8Array 分支一致取子窗口并越界校验 SOURCE_ERROR。
  3. **mse-helper 失败码映射**：SourceBuffer 更新失败→DECODE_ERROR(:51)、不支持 mime/无
     MediaSource→NOT_SUPPORTED(:112/:159)；导入同步更新。代码修复（模块无单测，集成覆盖于 hls mse-controller 测试模式）。
- 测试：新增 2 例（readUEG 31 前导零→4294967294；DataView 子窗口 length=4），全仓 **869/869 全绿**。
- 台账新增第 10 章「第五批复核记录」：§10.1 已闭环补销账、§10.2 三处修复、§10.3 仍真实开放候选。
- 复盘：评审台账与代码漂移明显，后续每波先 grep 核验再动手，避免对已闭环项重复劳动。

## round-1 评审第六波：hls BYTERANGE 修复 + §10.3 现盘复核

- §10.3 候选现盘复核：hls `EXTM3U` 首行校验（m3u8-parser:97-103）已闭环；`mfhd`/`_seq` 序号递增活跃（非死代码，误报）；
  `tsAvailable` 是静态能力探测，但 fmp4-muxer 静态 import ts/nalu 使其降级承诺失真（架构项，未修）；
  `TARGETDURATION` 仅读取未校验（低优先仍开放）；`BYTERANGE length=0` 确认仍真实。
- 本轮修复（hls 建议项，实证）：`m3u8-parser.js` BYTERANGE 标签(:359-368)与 EXT-X-MAP 属性(:379-384)新增
  `br.length>0` 校验抛 PARSE_ERROR；`segment-loader.js:98-99` 改为 `byteRange.length>0` 才拼 Range 头（防御兜底）。
- 测试：新增 2 例 hls 回归用例（解析期 PARSE_ERROR + 加载器跳过非法 Range 头），hls 74/74、全仓 **871/871 全绿**。
- 台账新增第 11 章（§11.1 修复、§11.2 §10.3 复核表、§11.3 回归）。

## round-1 评审第七波：core clock 契约偏差闭环 + §10.3 现盘复核

- §10.3 复核：transmuxer `kind==='unknown'` 静默空转为**误报**——`transmuxer.js:74-77` 已 `throw PlayerError(PARSE_ERROR,'无法识别的分片容器形态…')`。
- 本轮修复（core 两条）：
  1. `clock.js` `DEFAULT_SYNC_OPTIONS` `maxLateSec:0.12/maxEarlySec:0.048` → `0.02/0.02`，对齐 CONTRACTS §7 ±20ms（原 ±120ms/±48ms 属契约偏差）；新增回归用例断言 `DEFAULT_SYNC_OPTIONS.maxLateSec===maxEarlySec===0.02`。
  2. `stats.js:15` 默认钟 `Date.now()/1000` → `performance.now()/1000` 优先（Date.now 兜底），与 clock.js 口径一致，规避 NTP 回拨致 fps EMA 负 dt。
- 测试：core **60/60**；全仓（不带 `--test-force-exit`）**872/872 全绿**；带 `--test-force-exit` 偶发 flv/mp4/ts `cross-fixtures`、mp4 `artifacts-edge` 文件级 cancelled（fixture I/O 与强制退出竞速，fail 恒 0，单独 26/26 全绿），属预存 harness flaky，非本轮引入。
- 台账新增第 12 章（§12.1 修复、§12.2 §10.3 复核、§12.3 仍开放、§12.4 回归）；绿色计数以「不带 --test-force-exit」的稳定值 872 为准。
- §10.3 仍开放：tsAvailable 架构项、hls 长直播周期 remove、TARGETDURATION 校验、§10 注册形状、parseAttributes、credentials、level-controller stall、ts PCR/192/LATM、mkv Cues/Cluster/codecs。

## round-1 评审第八波：mkv mp3/vorbis codec 串修复 + §12.3 现盘复核

- §12.3 复核：parseAttributes(`utils.js:73-93`) 属性名大写字面匹配、引号值保留逗号、0x 保留、数字强转 → RFC 合规，**误报**补销账；§10 注册形状对 hls(非容器 demuxer) **N/A**；credentials `segment-loader.js:101` 硬编码 `credentials:'omit'` 为真实缺口（受保护 HLS 丢 cookie，需配置链路），留待特性波次。
- 本轮修复（mkv 真实 bug，实证）：`codecs.js` `normalizeCodec` 缺 `mp3`/`vorbis` 分支，且 CODEC_TABLE 中 `A_MPEG/L3`/`A_VORBIS` 标 supported 却无 entry.codec → `out.codec` 回退原生 Matroska CodecID(`A_MPEG/L3`/`A_VORBIS`)，`demuxer.js:475` 透传给 MSE 致非法 codec 串、MP3/Vorbis 音轨无法解码。
  修复：新增 `case 'mp3'→'mp3'`、`case 'vorbis'→'vorbis'`（Vorbis 缺 CodecPrivate 仅告警）；新增 `mkv/__tests__/codecs.test.js`（4 例）。
- 测试：mkv **73/73**（+13 / 本波+4）；全仓（不带 `--test-force-exit`）**876/876 全绿**。
- 台账新增第 13 章（§13.1 修复、§13.2 §12.3 复核、§13.3 仍开放、§13.4 回归）。
- §12.3 仍开放：tsAvailable 架构项、hls 长直播周期 remove、TARGETDURATION 校验、credentials 配置化、level-controller stall、ts PCR/192/LATM、mkv Cues/Cluster 双遍历。

## round-1 评审第九波：ts LATM ASC 启发式错位修复 + §13.3 现盘复核

- §13.3 复核：ts 192 误锁 — `ts-stream-engine.js:195-224` 检测顺序互斥（M2TS 分支要求 i>=4+i+192/i+2*192 同步，纯188流不满足；[188,192] 先试188），固定格式流不可误锁；仅首窗垃圾恰现 192 步长三同步巧合的罕见边界不可自愈 → 仍开放（低优先）。
- 本轮修复（ts 真实 bug，实证）：`aac.js` `tryReadAscBounded` 旧版宽松字段范围判断（`aot∈[1,6]/idx≤15/ch∈[1,7]`）会把音频数据巧合位误锁为 ASC，且对 `sampling_frequency_index=13/14`(保留)/`=15`(escape 需额外24位频率)透传 —— idx=15 返回仅 2 字节 ASC，下游 `parseAudioSpecificConfig` 越界/产错采样率。
  修复：候选 ASC 改交 `parseAudioSpecificConfig` 真校验（sampleRate 定义、ch∈[1,7]）；显式拒 idx===13/14，idx===15 因窗口不足 parse 越界抛错走降级；优先 delta=0 精确位。实证：aot=7 正确提取 44100；idx=15 不再返回 `[0x17,0x90]`。
  新增 `ts/__tests__/aac.test.js` 2 例（AOT=7 精确提取 + idx=15 不被误锁为 idx=15 ASC）。
- 测试：ts **65/65**（较八波+2）；全仓（不带 `--test-force-exit`）**878/878 全绿**。
- 台账新增第 14 章（§14.1 修复、§14.2 §13.3 复核、§14.3 仍开放、§14.4 回归）。
- §13.3 仍开放：tsAvailable 架构项、hls 长直播周期 remove、TARGETDURATION、credentials 配置化、level-controller stall、ts PCR 未提取、mkv Cues/Cluster 双遍历、ts 192 误锁罕见边界。

## round-1 评审第十波：ts PCR 提取 + 时长兜底 + §14.3 现盘复核

- §14.3 复核：hls 长直播周期 remove — `mse-controller.js:98-101` 仅 `buffered.length>=30` 触发 `_onQuotaEvict`（被动），无周期 remove → 仍开放（长会话内存增长；需 player 当前时间驱动 trim，非一行可修）。
- 本轮修复（ts 准确性缺口，实证）：`ts-stream-engine.js` `_parsePacket` 旧未读 PCR（flags 字节 `0x10` PCR_flag，`pkt[6..11]` base33+ext9 位 90kHz），时长只能靠 DTS 跨度。
  修复：① `_parsePacket` 在 AF-PCR 位置提取，`_recordPcr` 解码 base(33位,**不用 `>>>0` 截断**)/ext(9位)→pcr90k，跟踪 `_pcrFirst/_pcrLast`（discontinuity 重置），emit `'pcr'` 事件 `{pid,pcr90k,discontinuity}`；② `_emitMetadata` 新增 PCR 跨度兜底时长（DTS 优先、PCR 兜底，33位回绕处理），metadata 带 `pcrDurationMs/pcrSeen`。
  新增 `ts/__tests__/pcr.test.js`（4 例：提取+事件、PCR 跨度兜底、DTS 优先、不连续重置）。
- 测试：ts **69/69**（较九波+4）；全仓（不带 `--test-force-exit`）**882/882 全绿**。
- 台账新增第 15 章（§15.1 修复、§15.2 §14.3 复核、§15.3 仍开放、§15.4 回归）。
- §14.3 仍开放：tsAvailable 架构项、hls 长直播周期 remove、TARGETDURATION、credentials 配置化、level-controller stall、mkv Cues/Cluster 双遍历、ts 192 误锁罕见边界。

## 第十一波（hls 长直播/EVENT 周期缓冲回收）— §15.3 闭环

- 复核 §15.3「hls 长直播/EVENT 周期 remove」：实码确认 `mse-controller.js:98-101` 仅 `buffered.length>=30` 被动 `_onQuotaEvict`，无周期回收 → 真实缺口。
- 修复：
  - `hls/src/mse-controller.js` 新增 `trim({behindSec=30, aheadSec=30, currentTime})`：仅移除整段落在 `[t-behind, t+ahead]` 窗口外的历史缓冲，部分重叠保守保留；缺省回退 `video.currentTime`；无播放点安全空返。
  - `hls/src/player.js` 新增 `backBufferSeconds`(默认30) 配置 + `_maybeTrim()`：`_pump` 内对 `pl.live` fire-and-forget 调用，`_trimming` 重入保护，不阻塞 append 流水线。
  - 门槛正确性：`m3u8-parser.js:515` `live=!hasEndlist && playlistType!=='VOD'` → EVENT 未收尾=live(回收)、收尾=VOD(不回收保 seek)。
- 测试：新增 `hls/__tests__/mse-trim.test.js`（6 例）；`hls/__tests__` → **80/80**；全仓入套 850 通过 + 4 文件 cancelled(flv/cross-fixtures、flv/remuxer、hls/ts-remux、mp4/artifacts-edge，fixture 重 I/O 竞态，隔离复跑 35/35 全绿)= **885 全绿 fail=0**，与本轮改动无关。
- 台账新增第 16 章（§16.1 修复、§16.2 §15.3 复核、§16.3 仍开放、§16.4 回归）。
- §15.3 仍开放（下一波候选）：tsAvailable 架构项、TARGETDURATION 违例、credentials 配置化、level-controller stall 快速降档、mkv Cues 线性扫/Cluster 双遍历、ts 188/192 误锁自愈。

## 第十二波（hls/ts load 期解耦 + tsAvailable 语义修复）— §16.3 架构项闭环

- 复核 §16.3「tsAvailable 降级承诺失真」：`transmuxer.js:99` 承诺探测降级，但 `fmp4-muxer.js:17` 静态 import ts/src/nalu → ts 缺失时整条 hls 模块图加载失败，探测执行不到 → 真实架构缺口。
- 修复（`hls/src/fmp4-muxer.js`）：
  - nalu 改模块级惰性动态加载：`loadNalu()` 缓存 promise 幂等、模块加载即预热；`toAvcc` 同步接口不变（经 `_nalu` 访问，未就绪抛明确错误）。
  - `remux` 入口 `await loadNalu()` 后再 demux。
  - `remux` 内 ts 导入失败裸抛 → `PlayerError(NOT_SUPPORTED)`（契约禁止裸 Error）。
  - 核实：TsDemuxer 本就动态导入，nalu 是 load 期对 ts/ 的最后一处静态依赖。
- 测试：新增 `hls/__tests__/lazy-nalu.test.js`（4 例：tsAvailable 语义、loadNalu 幂等、真实 TS 全链路、源码守卫禁恢复静态 import）；`hls/__tests__` → **84/84**；全仓两轮 fail=0，cancelled 文件每轮名单不同（负载相关竞态），12 个唯一文件隔离复跑 108/108 全绿 → 有效 892 例全绿。
- 台账新增第 17 章。
- §16.3 仍开放：TARGETDURATION 违例、credentials 配置化（segment-loader.js:101 硬编码 omit，已实证待修）、level-controller stall、mkv Cues/Cluster、ts 188/192 误锁自愈。

## 第十三波（hls credentials 配置化）— §17.3 闭环

- 复核 §17.3「credentials 配置化」：`segment-loader.js:101` 全仓唯一 fetch 调用点硬编码 `credentials:'omit'`（loadText 委托 load）→ 带 Cookie 授权源无法接入，真实缺口。
- 修复：
  - `SegmentLoader` 新增 `credentials` 选项（omit/same-origin/include，缺省 omit 向后兼容；非法取值构造期抛 `PlayerError(STATE_ERROR)` 不静默）。
  - `load()` 改用 `this.credentials`。
  - `HlsPlayer` 新增 `config.credentials` 贯通到 loader，JSDoc 注明授权源用法。
- 教训：credentials.test.js 首版 import 路径多写一级（`../../../core`），`hls/__tests__/` 下 core 应为 `../../core`。
- 测试：新增 `hls/__tests__/credentials.test.js`（6 例）；`hls/__tests__` → **90/90**；全仓 fail=0，16 文件 cancelled（负载竞态，前几波同池），6 个新增名单隔离复跑 30/30 全绿 → 有效 898 例全绿。
- 台账新增第 18 章。
- §17.3 仍开放：TARGETDURATION 违例、level-controller stall 快速降档、mkv Cues/Cluster 双遍历、ts 188/192 误锁自愈。

## 第十四波（hls stall 快速降档）— §18.3 闭环

- 复核 §18.3「level-controller stall 快速降档」：头部注释承诺「缓冲<2s 立即降档」但 `autoSelect` 降级全靠带宽 EWMA 回落、`bufferSeconds` 仅升级闸；player 无任何 video 事件监听 → 注释/实现不符，真实缺口。
- 修复：
  - `LevelController.handleStall(minGapMs=2000)`：绕过 EWMA 与升级闸直切最低档；仅 auto 模式（手动档不覆盖）；lastSwitchTime 节流防连环。
  - `HlsPlayer`：`video 'waiting'` → `_maybeStallDowngrade()`，前置闸（destroyed/无 levels/paused/缓冲>1.5s）；切档用 afterSn 锚点续播；destroy 解绑（先于 mse.destroy，因其清空 video）。
  - stall 后 autoLevelEnabled 保持 true，reportLoad ABR 复核可爬回。
- 测试：新增 `hls/__tests__/level-stall.test.js`（7 例）；教训：默认 EWMA(256k) 下 stall 到底后本就不该爬升，断言需注入高带宽估计桩；`hls/__tests__` → **97/97**；全仓 fail=0，7 文件 cancelled（前几波已隔离验证）→ 有效 905 例全绿。
- 台账新增第 19 章。
- §18.3 仍开放：TARGETDURATION 违例（低优先容错）、mkv Cues 线性扫/Cluster 双遍历、ts 188/192 误锁自愈。

## 第十五波（mkv open 惰性停止，消除双遍历）— §19.3 闭环

- 复核 §19.3「mkv Cues 线性扫/unknown-size Cluster 双遍历」：`locate()` 二分已由重构闭环（现行双分支均二分）；「open 全量扫簇 + 无 Cues seek 二次扫」双遍历仍在 → 真实缺口。
- 修复（`mkv/src/demuxer.js`）：
  - open 顶层扫描在**首个 Cluster 惰性终止**（新增 `_infoSeen/_tracksSeen`，仅 Info/Tracks 已见才停；非规范顺序回落旧全扫）。
  - `#ensureSeekIndex` 扩展为同时**惰性发现尾置 Cues**（扫簇头命中 Cues 即 parseCuesAt 后走 Cues 二分），簇后区间仅首次 seek 扫一次。
  - 插桩实证（读范围间谍）：open 后 maxRead 止于首簇头、cues 空；拉流 3 簇全解；seek 后 cues=3。
- 连带：修正 demuxer.test「SeekHead→Cues」fixture 偏移少加 seekHead.length 的错误（旧全扫掩盖）；改定宽 8B SeekPosition 两趟构造。教训：断言绝对值与段内相对偏移比较必须加 segmentDataStart。
- 测试：新增 `mkv/__tests__/lazy-open.test.js`；`mkv/__tests__` → **74/74**；全仓 fail=0，7 文件 cancelled（前几波已隔离验证）→ 有效 906 例全绿。
- 台账新增第 20 章。
- §19.3 仍开放：TARGETDURATION 违例（低优先容错）、ts 188/192 误锁自愈。

## 第十六波（ts 188/192 误锁自愈重探测）— §20.3 闭环

- 复核「188/192 误锁后不可自愈」：三同步点锁定后永不重探，主循环失步仅 pos++；首窗垃圾 192 三同步巧合误锁即全损（实证：垃圾 1300B+真流 → packetSize 锁死 192、samples=0）。
- 修复（`ts/src/ts-stream-engine.js`）：
  - 新增 `resyncs` 诊断计数。
  - `_consume` 外层循环化：主循环跟踪连续失步 `slid`（命中归零），`slid ≥ 4×packetSize` → 丢弃失步窗口、重置包长重新探测（warn + resyncs++），成功后继续。
  - 实证：重锁 188、resyncs=1、3 样本全出；短抖动/分块喂入不受影响。
- 测试：`ts/__tests__/ts-edge.test.js` +3 例。教训：① fixture 数组要展开别嵌套（concatBytes 把子数组当数值展开成 0）；② 非整包垃圾横切 TS 流会连累前一真包被双校验滑掉（固有语义），短暂失步用例须整包颗粒插入。
- 回归：`ts/__tests__` 72/72；全仓默认并发 fail=0 但 cancelled 随负载漂移 → **`--test-concurrency=4 --test-timeout=15000` 909/909 全绿 0 cancelled**（906+3）——后续全仓建议低并发参数。
- 台账新增第 21 章。
- §20.3 仍开放：TARGETDURATION 违例（低优先容错）、Transmuxer.tsAvailable 降级承诺失真（架构项）、ts PCR 未提取（时长退化）。

## 第十七波（hls TARGETDURATION 违例自愈校验）— §21.3 闭环

- 复核「TARGETDURATION 违例不校验」：`m3u8-parser.js` 仅 `Number(...)||0` 读声明（非法值静默归零）、`player.js:439` 直播轮询直接消费声明（target/2）——声明过小轮询过频、归零靠 ||6 猜测。
- 修复：
  - parser：parseMedia 尾部新增 RFC 8216 语义校验——EXTINF **四舍五入**到整数须 ≤ 声明（6.008 配 6s 合规，6.6 配 6s 才违例）；违例时自愈提升 `ceil(maxDur)` + 暴露 `targetDurationViolations`；声明缺失/归零同样按 maxDur 兜底。
  - player：`_checkTdViolation(pl)` warn 诊断，三处 mediaPlaylist 落库点接线。
- 教训：首版「裸比 EXTINF>声明」过严，被既有 VOD fixture（TD:6 + 6.008s）打回——规范判定是四舍五入；合法 fixture 本身就是规范校准器。
- 测试：新增 `hls/__tests__/td-violation.test.js` 5 例；`hls/__tests__` **102/102**；全仓低并发 **914/914 0 cancelled**（909+5）。
- 台账新增第 22 章。
- 仍开放：Transmuxer.tsAvailable 降级承诺失真（架构项）、ts PCR 未提取（时长退化）。

## 第十八波（全景收官：EXTINF 前置 BYTERANGE + 陈旧项纠正）— 候选池清零

- 复核 §22.3 两项：ts PCR 未提取 / tsAvailable 降级失真——均已闭环（前者 §15.1 _recordPcr+metadata 兜底+pcr.test；后者架构项 §16.3 惰性动态 import + lazy-nalu 源码守卫测试），是台账滚抄陈旧文本，纠正。
- 全景清单（台账首行）逐项核对 → 唯一遗漏真实缺口：**EXTINF 前置 BYTERANGE 丢失**（m3u8-parser 归属仅 `if(current)`，前置排列 current=null 时 byteRange 不挂任何分片）。
- 修复：`pendingByteRange` 暂存，前置 BYTERANGE 挂给下一个 EXTINF 分片（EXTINF 前 flushSegment 时序保证归属正确）；缺省 offset 滚动语义跨排列不变。
- 测试：`hls/__tests__/byterange-prefix.test.js` 4 例。教训：BYTERANGE 语法无逗号，测试 fixture 从 EXTINF 复制逗号导致解析失败（两处踩坑）。
- 回归：`hls/__tests__` 有效全绿；全仓低并发 **918/918 0 cancelled**（914+4）。
- 台账新增第 23 章含**全景收官矩阵**（首行清单 24 子项逐项终态 ✅/N/A/边界）。第一轮滚动评审候选池清零，可转收官报告或进入第二轮新扫描面。

## 第十九波（checklist 契约一致性面现盘复核）— 发现 F 表严重陈旧

- 第十八波解析层候选池清零后，回头核 `docs/review/checklist.md` 正式评审面：F 系列预扫描（2025-08-25）大半 ⬜ 待评但实码早已落地。
- 全量现盘证据：G9 errors 十码+十快捷构造器齐；F2 logger.js 四级日志齐；F3 capabilities hasWebGPU/hasCryptoSubtle/chooseRoute/detectCapabilities 齐；F4 http-range-source.js + data-source.js:79 ChunkBuffer 齐；F6 audio-worklet-player.js:93 注册名已改 'player-audio-sink'；F9 ts-demuxer.js:42 **TsDemuxer extends Demuxer** 基类对齐样板已落地。
- 真开放项全部归第二轮 I1（M3 末触发）：F7/F8 渲染端工厂（createAudioOutput/createVideoRenderer 未实现，仅类导出）；F10 mkv extends Emitter 未继承基类（**全仓唯一落单**，ts 为样板）；F11 hls EventBus 平行件（watch）。
- 修改：mkv/demuxer.js 头注释陈旧纠正（E-8 波次已完成，指向 I1）；checklist.md F 表状态列全量更新（带现盘证据）。
- 里程碑判断：第一轮实质收官；架构级改造（mkv 基类继承等）应等 M3/I 系列正式开波，勿越权。
- 工具教训再确认：**macOS BSD grep 不支持 `\|` alternation**（此前"偶发空输出"根因），bash 一律单模式或改用 Grep 工具。
- 台账新增第 24 章。

## 第二十波（F10 mkv 基类对齐预研笔记）— 交付 docs/review/mkv-base-class-alignment.md

- 深读 core/src/demuxer.js(412行) 基类骨架与 mkv/src/demuxer.js(987行) 自含实现 + ts 样板，产出 I1 前置预研笔记。
- 关键发现：
  - 同构：mkv #iterateTrack ≡ 基类 _createTrackIterator；open 内核 ≡ _doOpen；seek 体 ≡ _doSeek。
  - **机械替换必挂 3 组既有断言**：①未 open 访问 tracks/mediaInfo 同步抛(基类返 null/[])；②samples() 未 open 同步抛(基类是懒生成器，ts 已接受懒语义 ts-demuxer.test:187-190)；③end 需全部可读轨拉完恰一次(基类已拉轨全 done 即发可多次)。
  - mkv 守卫/end 语义比基类与 ts 更严、贴契约 §2.3 字面 → 真实跨模块语义分歧，I1 裁决面 Q1-Q6。
- 三案设计：A 贴基类改测试 / B 基类+override 零回归 / C 形式继承保自实现（推荐先 C 后 A）。
- 零代码改动；mkv 74/74 基线不动。
- 台账 §25；笔记路径 docs/review/mkv-base-class-alignment.md。

## 第二十一波（G 系列通用红线跨模块系统核查 + mp4 写端收口）

- 前 18 波只闭环了解析层细节；checklist §1 G 系列十条"每模块必查"从未系统登记。本波 16 模块一次扫清。
- 结论：G1/G2/G3/G5/G7/G8/G10 全绿（脚本实证）；G4 导出 JSDoc 齐（§10 元数据常量组共享 section 注释，不补噪音）；G6 918/918 基线；**G9 三分法**——①内部防御原语豁免（amf0/bits/lacing 入口自收口或触发即 bug）②渲染端/传输层裸 Error 归 I1(F7/F8)/I5（webrtc player:77/121、rtsp client:377、core 渲染端 4 处）③**本轮实修 mp4 写端 4 处**（remuxer 缺 description/空 samples → stateError；box-builder 未知 sample entry → notSupported、moof 无 trun → parseError）。
- amf0 实证细节：裸 Error 全在模块内 catch（decodeAmf0/All 入口 try），flv-demuxer 各 parser.push 点 catch → emit('error')，对外面无泄漏。
- 工具教训复确认：BSD grep `\|` 空转（改用 Grep 工具）；复杂 JS 脚本 heredoc 落盘会被外层 zsh 解析 `${...}` → 用 Write 直落 /tmp。
- 回归：mp4 套件全绿；全仓低并发 **918/918 0 cancelled**（核查波无新增用例，改动由既有断言覆盖）。
- checklist.md §1 更新为逐条 ✅/⏳ 带实证；台账新增第 26 章。

## 第二十二波（初始清单收官审计 + flac 契约面补齐）— §1-§7 全部清零

- 对台账 §1-§7 初始正式清单（ts/core/mkv/webtorrent/hls/wav/subtitle）做全量收官审计：逐条磁盘证据核证 → **全部清零**（早期 2026-08-25/26 批次 + 前 21 波覆盖）。
- §10 注册形状 16 模块扫描 → **flac 是 demux 容器唯一落单**（同族 wav/mkv/ts/mp4/flv/mov 五件套齐；ape 非 demuxer 合理；hls Player 面合理；传输层 createSource 合理）。
- flac 修复：
  - index.js：§10 五件套（containerName/extensions/mimeTypes/probe/createDemuxer 工厂）。**教训：纯 re-export `export { X } from` 无本地绑定，模块内引用 X 会 ReferenceError**（首跑即暴露）→ import+export。
  - demuxer.js：open()/readSample()（共享 #reader 单拉 EOS null）/destroy()（幂等置 destroyed）定稿名。
  - seek ended→ready（**wav#2 同款潜伏 bug**：播完 seek 后 samples 永久 STATE_ERROR，wav 早修而 flac 留存）。
  - 测试 contract.test.js 6 例。
- **纠正 F10 表述**：mkv 非唯一落单——flac 同样不继承 core Demuxer；I1 基类对齐面 = mkv+flac 两个，mkv-base-class-alignment.md 三案设计对 flac 同构适用。
- 回归：flac 46/46；全仓低并发 **924/924 0 cancelled**（918+6）。
- 台账新增第 27 章。至此初始清单、G 系列、F 系列全闭环/登记；剩余全在二轮 I 系列（含扩为 mkv+flac 的基类面）。

## 第二十三波（第二轮 I1 首波：core 渲染端工厂形态对齐）

- 进入第二轮 I1，先闭环 F7/F8 渲染端公共 API 形态，不擅动播放管线与 mkv/flac 基类架构。
- `core/src/audio-worklet-player.js` 新增 `createAudioOutput({sampleRate,channels})`，兼容 `channelCount`；已有 `currentTimeUs`、`underrunCount`、`player-audio-sink` 保持。
- `core/src/video-frame-renderer.js` 新增 `createVideoRenderer(canvasEl,{preference,fit})`，兼容 `mode`；实现 `contain/cover/fill` 几何语义，默认 contain。
- `core/src/index.js` 补导出两个工厂；`core/__tests__/review-fixes.test.js` 新增 2 例。
- 定向 core 回归 14/14，渲染专项复跑 5/5，全绿；全仓低并发回归 **926/926 全绿，fail=0，cancelled=0**。
- G9 延伸收口：渲染端对外环境/生命周期错误改用 core 十码，内部 shader 防御断言保留；checklist F7/F8 与台账 §28 登记闭环。
- 下一候选：F10（mkv+flac）基类继承/契约语义精比对，随后 I5 传输层输入校验与错误收口。

## 第二十四波（第二轮 I1 第二波：mkv + flac 继承 core Demuxer）

- F10 范围按前波纠正扩为 mkv + flac；两者均接入 core Demuxer，保留已固化的严格 getter、EOS、seek 兼容语义。
- mkv：改为 `extends Demuxer`，调用 `super(source, options)`，内部改用基类 `stateValue`，规范化 `dataSource` 回挂 `this.source`。
- flac：改为 `extends Demuxer`，使用 core emitter；解析拆为 `_doOpen()`/`_parseInit()`，保留 `parseInit()`、FLAC 专属 metadata getter 与 readSample/samples/seek 兼容行为。
- 回归：mkv **74/74**、flac **46/46** 全绿；checklist F10 与台账 §29 已登记闭环。

## 第二十五波（第二轮 I1 第三波：D1-D12 契约差异裁决）

- 对照 core Demuxer、mkv/flac 当前实码与 CONTRACTS §2.2/§2.3，完成 D1-D12 证据化裁决；明确继承基类与完全语义同构分层验收。
- 保留已固化兼容语义：mkv open 重入抛错、open 失败回 idle、未 open getter/samples 同步守卫、全部可读轨 EOS 且单次 end；不对运行时行为做破坏性改动。
- 新模块/后续统一方向：core 共享 open promise、十码错误、基类默认 getter/懒 samples、直播 pause/resume 事件；pull 消费不依赖 sample 事件，外部只依赖 state。
- 详细 D1-D12 矩阵已写入 docs/review/mkv-base-class-alignment.md §8，台账新增 §30。
- MKV+FLAC 定向回归 **120/120 全绿**；本波无运行时代码变化，沿用全仓 **926/926 全绿**基线。
- 下一候选：I5 传输层输入校验与 PlayerError 收口（webrtc/rtsp）。

## 第二十六波（第二轮 I5 首波：WebRTC/RTSP 输入校验与错误收口）

- WebRTC：播放地址非空校验、非法协议/状态迁移/缺少 RTCPeerConnection 分别收口为 PARSE_ERROR/STATE_ERROR/NOT_SUPPORTED；WHEP 与 WebSocket 信令错误收口为十码。
- RTSP：RTP 畸形输入、RTSP 响应失败、SDP HTTP 失败、interleaved 缓冲超限统一收口为本地十码 PlayerError；补充 source/notSupported 快捷构造器。
- 保留 mock-signaling、Base64 能力防御等内部/测试边界裸 Error，不让其作为用户输入错误逃逸。
- WebRTC + RTSP 定向回归 **67/67 全绿**。
- 第二十七波：F11/F12 事件面裁决完成。HLS EventBus 改为 core Emitter 别名，RTMP MiniEmitter 改为 core Emitter 兼容别名，RTSP ChunkSource 直接继承 core Emitter；core Emitter 补齐多参数 emit/once 语义。新增 event-contract.test.js，事件相关回归 184/184 全绿；TS/FLV 内部解析 emitter 保留为隔离实现。
- 第二十八波：完成 I3/I4/I6 首轮审计。16 个功能模块均具备 README、src/index.js、demo/index.html 和测试；site 导航补入 core 入口；根 README 模块状态表由 M1 占位更新为当前实盘状态，并明确桥接模块完成边界。相关台账已记入 §32。
- 第三十三波：收尾 I3/I4/I6 入口与验收文档一致性。site/index.html 改为无脚本即时跳转正式汇总页并保留后备链接；docs/test/TESTPLAN.md 去除骨架版标记，更新 16 模块状态为“解析层与 demo 骨架已落地”，并保留浏览器端到端/网关联调的条件验收边界。台账已记入 §33。
- 第三十四波：完成第二轮收尾基线。G9 更新为闭环；一次性传入 110 个测试文件会被环境 SIGTERM，改用显式展开的全部 JS/MJS 测试文件串行执行，得到 929/929 全绿、fail=0、cancelled=0、skipped=0（约 224 秒）；台账已记入 §34。
- 第三十五波：预审 I2 播放管线，确认 CONTRACTS §5 定义的 createPlayer/Player 编排层在 core/src 尚未实现，现有 chooseRoute/createDemuxerAuto/AvSyncController 尚未串接；core demo 仅演示基础能力，不能冒充端到端播放。将 I2 保留为下一波实质开发候选，台账已记入 §35。
- 第三十六波：新增 core/src/player.js 最小播放编排层，串接 demuxer 自动探测/打开、能力路线裁决、可注入 pipeline、play/pause/seek/destroy、样本泵、状态与事件；core index 补导出，新增 player.test.js 4 例全绿；真实 WebCodecs/MSE/Canvas/AudioWorklet 仍保留浏览器 M3/M4 条件验收边界，台账已记入 §36。
- 台账新增 §31；下一步运行全仓回归并观察 F11/F12 平行事件/错误面。
