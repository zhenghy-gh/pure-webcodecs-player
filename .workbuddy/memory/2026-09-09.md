# 2026-09-09 工作日志

## 版本管理：项目此前从未纳入 git（重大发现）

用户问「代码修改都同步推送到 github 上了吗」，核实发现：

- 项目**根本没有 git 仓库**——只有 `.gitignore`（8/25 创建），无 `.git`，向上遍历到 `/` 也无任何仓库
- `gh` CLI 未安装
- 因此**前 49 波全部改动从未提交、从未推送**，只存在于本地磁盘，无提交历史也无远端备份

用户选择「初始化并本地首提交（推荐）」，执行结果：

- `.gitignore` 补两条规则：
  - `samples/e2e/`（脚本可再生媒体产物，32M，占项目总量 38M 的绝大部分；目录内无任何 .js/.mjs 源码）
  - `.smoke/`（8/26 一次性排查草稿 14 个 `dbg-*`/`t7*` 脚本，全仓无引用；**仅排除入库，磁盘文件保留**）
- `git init` → 首提交 `4dc220d`（branch `main`），**435 files changed, 71624 insertions**
- 注意：`git add -A` 后再加忽略规则不会自动清出索引，需用 `git rm -r --cached <dir>`（只退暂存、不动磁盘）才能移除已暂存的 `.smoke`
- 工作区提交后 clean；未推送（按用户规矩，push 须显式确认）

## 第五十波（进行中）：§2.4 契约对齐全 16 模块核对

背景：`docs/review/checklist.md` §2.4 八项从未被任何波次正式核对（PRD 第 3 项即为该 reviewer 任务）。

新建 `scripts/audit/contract-2-4-audit.mjs`（结构层全模块审计：类名/继承/ownProbe/ownDoOpen/probe 同步·不抛·垃圾返 null/方法面/别名/default 导出）。

初版启发式三处误报已修正：类名正则需允许数字（`Mp4Demuxer`）、只认真 class（排除 `registerDemuxer` 同名函数）、base/source scope 不应套 demuxer 口径。

现盘结论（结构层）：

| 模块 | 结果 |
|---|---|
| mp4 / mov / ts / flv / flac | ✅ 全项通过（继承基类、自实现 probe+_doOpen、方法面齐全） |
| mkv | ⚠️ 继承基类但**直接覆写 `open()`（demuxer.js:237），未实现 `_doOpen`** → 绕过基类编排（initTimeoutMs 超时、状态机 OPENING→READY、自动 emit media-info、tracks 排序） |
| wav | ⚠️ `WavDemuxer` **未继承 core Demuxer**、无 `_doOpen`、**缺 pause/resume** |
| cmaf / hls / subtitle | 非 demuxer 类模块（cmaf 导出 probe 等函数；hls 是 HlsPlayer 整播放器；subtitle 是文本解析+渲染器）→ 属我 scope 划错，§2.4 不适用，非代码缺陷 |
| rtmp | 自含 `FlvDemuxer`（传输联调用，接口对齐 flv 适配壳），scope=source，§2.4 基本 N/A |
| ape / webtorrent / webrtc / rtsp | 无 demuxer 类导出，符合 probe-only / source 定位 |
| core | 基类自身，不适用 |

## 第五十波收口（同日）

查证与修复结果：

**mkv**（`mkv/src/demuxer.js` open() 覆写绕过基类）
- 无 `initTimeoutMs` 超时：构造函数 `this.options = options`(:121) 覆盖基类默认值，且自编排 open() 无 Promise.race → 卡死源永久挂起。已补超时守卫（回落缺省 10000ms，finally clearTimeout，超时回退 idle 允许换源重试）
- 只发 'media-info' 未双发旧名 'mediaInfo'（基类 :151-152 双发；mkv-base-class-alignment.md D8 裁决「增发旧名」未落地）。已补双发

**wav**（`wav/src/demuxer.js`）
- 未继承基类属**批准适配壳**：文件头 :5-7 留档「暂不继承 core BaseDemuxer（共享看板约定，解析层先行）」→ 豁免
- 缺 `pause()/resume()`（§2.4 第 6 项，仅 start() 标【可选】）→ 已补（置 pausedFlag + emit）
- `parseInit()` 无超时 → 已补（构造函数接 initTimeoutMs，Promise.race + timeoutError）

验证：新增 4 例防回归（mkv 超时+双发、wav pause/resume+超时）；mkv 24/24、wav 27/27、全仓 **1015/1015 fail=0 cancelled=0**。

**踩坑（测试侧，重要）**：超时定时器按 core 同款 `unref()`，而永不 settle 的 read promise 不持有事件循环 → loop 提前排空，timeout 不触发，报 `Promise resolution is still pending but the event loop has already resolved` 并**级联 cancelled 后续 15 个用例**。实现不动，测试侧 `setInterval` 保活解决。

未完成：§2.4 第 4/5/7/8 项（STATE_ERROR / SEEK_UNSUPPORTED / 事件名恰五者 / 整数 µs）逐模块运行时核对。

## 第五十波 #22 收口（同日）：§2.4 第 4/5/7/8 项逐模块运行时核对

新建 `scripts/audit/runtime-2-4-audit.mjs`：对 mp4/mov/ts/flv/mkv/flac/wav 七个 demuxer，调用各自 `fixtures/gen.mjs` 现盘生成 canonical fixture（`__tests__/fixtures/` 已 gitignore）→ `MemoryDataSource` 装入 → 真实 `open()→readSample()` 循环，现盘核对四项：
- C4 未 open 调 readSample → STATE_ERROR（同步 throw / reject 均判）
- C5 seek：可寻址 resolve 0µs；无索引（ts）拒 SEEK_UNSUPPORTED
- C7 事件名 ⊆ {error, media-info, mediaInfo, sample, progress, end}
- C8 所有 Sample.timestamp/duration 整数 µs

**结果 7/7 全 PASS**。触发事件无溢出（mkv/flac/wav pull 模式无 sample，符合「sample 仅 start() 直播」）；ts 触发 progress（读流进度）合法。

本波仅审计+台账，**无源码改动**（mkv/wav 修复已在 `60419a7`）。§2.4 八项契约全模块闭环。台账：checklist §2.4 那条「未完成」已改写为收口结论；round-1 新增 §51。## 第二轮正式输出：round-2-问题清单.md（2026-09-09）

`docs/review/checklist.md` §5.2 规定「第 2 轮触发条件：M3 区块 t18 管线/t19 demo/t20 汇总页 ✅ → 输出 `docs/review/round-2-问题清单.md`」。核实三项触发条件均已达（§43/§44 管线、§48 demo 16/16、§47 汇总页），但该文件从未产出——**I 系列全部波次（§28–§51）此前都记在 round-1-问题清单.md 里**。

已新建 `docs/review/round-2-问题清单.md`：按 §0 格式（`[级别] 模块 文件:行号 —— 问题 —— 建议 —— owner —— 状态`）条目级汇总第二轮，定级 **阻断 17 / 严重 23 / 建议 6 = 46 条全部闭环**；逐波明细仍指向 round-1 §28–§51。阻断项全部来自真机端到端（I2 双管线 11 + I3 demo 4 + transmux 2）——Node 单测无法暴露，是引入浏览器真机基建的直接收益。checklist §5.2 已标注触发并输出。

同时：仓库已无未闭环 TODO（src 仅 mkv 一句已完成存档注释），checklist 全量检查项无未勾选项。下一轮候选写在 round-2 §10。

## 重要教训：差点违反既有裁决做重构（round-2 §10 纠正）

我在 round-2 §10 把「mkv 从覆写 `open()` 回归基类 `_doOpen`」列为下一轮候选，动手前读源文件+裁决文档救了场——**该重构违反现行裁决**：

- `docs/review/mkv-base-class-alignment.md:148` **D2 裁决**：open 失败「保留模块既有可恢复性，mkv 回 idle」，明令「**禁止单模块擅改**」；:78 注明改回基类 destroyed 会「令 mkv 现有 attach 换源重试路径失效」
- `:126` 「**不建议跳过 C 直接 A**」；`:160` F10「继承基类」与 D1-D12「完全同构」是**两个不同验收层级**，后者属跨模块统一议题
- 另发现两处我此前的误判：①mkv `:387` 自带 `TYPE_SORT_ORDER` 排序轨道（已核实与 core `TRACK_TYPE_ORDER` 相对顺序一致：video/audio/text/metadata = 0/1/2/3），所谓「无 sortTracks」是误判；②mkv 头注释 :11-18 陈旧，仍写「继承基类归 I1 待办」，与 §29 案 C 已落地矛盾

已执行：纠正 round-2 §10（该候选改为「须 captain 跨模块裁决后按案 A 收敛」+ 更正说明）；重写 mkv 头注释为准确现状，并加两条**防误删护栏**（open() 覆写是 D2/D1 裁决产物勿改；第五十波补的超时+双发看似与基类重复实为等价补齐，删则回退）。mkv 76/76 全绿，纯注释改动。

**可复用原则**：本项目动手前必查 `docs/review/*-alignment.md` 与 round 台账的「裁决」条目——很多看似缺陷的差异是**已裁决保留的兼容语义**，擅自"修"会造成能力退化。

## 第五十一波：readSample 接入可选 AbortSignal（全收口）

- 新增 `core/src/abort.js`：`raceAbort` / `throwIfAborted` 原语，基于既有 `abortedError`（ABORTED 码）；signal 为空零开销直通。
- 4 处 `readSample` 接入可选 `signal`：core 基类（demuxer.js）、mkv、flac、wav；`samples()` 糖层同步透传。mp4/mov/ts/flv 走基类自动生效。不传 signal 行为与冻结版完全一致（CONTRACTS §12.3「新增可选成员=允许」的合规演进）。
- 顺带修掉三个既有隐患：①core/mkv 异步生成器中断吞样本 → `entry.pending` / `#pendingResults` 缓存中断已落地样本，续读优先吐出，seek/destroy 清理；②flac 游标先于读取推进丢帧 → 改先读后推进；③wav 把 ABORTED 误判为故障置 error 态+emit → 排除 ABORTED。
- 语义：中断 reject `ABORTED`，不吞样本、不 emit('error')、不置 error 态（abort 是调用方控制流非模块故障）。
- 防回归：新增 15 例（core abort.test.js 11 + mkv 1 + flac 1 + wav 2），全仓 **1030/1030，fail=0**（基线 1015）。
- 文档：CONTRACTS §2.2 readSample 签名加 `options.signal?` + §12.3 演进记录；round-2 §52 台账。
- 提交：Wave 51 单提交（本地 main，未推送——远端未配置，推送需用户建 GitHub 空仓库给地址）。
