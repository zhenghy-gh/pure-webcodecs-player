# 2026-09-10 工作日志

## 第七十二~七十八波：薄弱模块测试补强 + mkv 两个真实缺陷修复

**背景**：owner 指令「继续100轮」，从第七十一波（e2e 脚本化）继续。基线 1144 测试全绿。

**做了什么**
- 用 6 个并行子 agent 为最弱模块补测（仅新增测试文件、不改 src）：webrtc(2→6 文件, 27→58 例)、wav(+4)、cmaf(+3)、ape(+3)、flac(+5)、mkv(+4)。
- 全仓测试 **1144 → 1385（+241），全量 1385/1385 绿**。
- 修复 mkv 两个真实缺陷（第七十二波 `8d47f09`）：
  1. **CueTime 未按 TimecodeScale 缩放**：`#parseCuesAt` 把 CueTime 直接当 ns，而 `locate()` 用 `targetNs = us*1000`，二者差 1e6 倍 → seek 二分命中错误簇。改为 `cueTimeTicks * this.timecodeScaleNs`。既有用例 demuxer.test.js「seek：Cues 定位」原期望 1.6s 命中 cluster1 是旧 bug 凑巧成立，已按正确语义把目标改为 2s（保持原意图）。
  2. **DiscardPadding 对外丢失**：`#emitBlockFrames` 已把 BlockGroup 的 DiscardPadding(sint,ns) 解析为 µs，但 `#iterateTrack` 映射层未透传 → `readSample` 拿不到 Opus/AAC 首尾填充裁剪依据。现透传 `discardPaddingUs`/`discardable`。

**关键教训（重要）**
- **Bash `grep` 对本仓库（路径含中文）会返回假阴性**——搜索 `DiscardPadding` 在 `mkv/src/` 明明有 3 处命中却返回空，一度误导判定「功能未实现」。本项目**一律使用 Grep 工具**，不要用 Bash grep。
- 子 agent 写的测试可能未经验证就落盘（429 中断），必须主进程跑全量复核。本次 mkv/flac 的 9 个文件全有缺陷，逐个修：
  - `Uint8Array.from(10)` 返回**空数组**（数字不可迭代），定长填充必须 `new Uint8Array(10)`。
  - Cues 自依赖：`CueClusterPosition` 值依赖 Cues 长度、Cues 长度又依赖该值 → 用 `EbmlWriter.u(id, val, minLen=8)` 固定宽度 + 两遍构造（占位 0 → 真实偏移）+ 等长断言兜底。
  - AAC AudioSpecificConfig 规范顺序：AOT(5) → sfi(4) → [sfi==15 时 24-bit 采样率] → channels(4)，显式采样率在 channels **之前**。
- 契约提醒：mkv **尾置 Cues 在 open 阶段不解析**（惰性，见 lazy-open.test.js），只有 seek 时才解析；需要 open 后断言 cues 的用例必须把 Cues 放簇前或用 SeekHead 接通。
- seek 落点语义：`#peekActualTimestamp` 找**首个 ≥ target** 的样本；若落点之后无样本则回退 `durationUs`（时长钳制），不是回退到最近簇。

**产出**：`8d47f09`(72) `c183b95`(73) `70a4010`(74) `fd2ef47`(75) `351616c`(76) `3c9e7e0`(77) `293ade7`(78)。

**下一步候选**：rtmp/subtitle/rtsp 补测（密度仍低）；P3 env 层可测化；P3 mkv 案 A 同构（待 owner 裁决，不擅动）。

## 并行后台任务批次（owner 指令：不要在主线程跑，起后台任务）

已启动 4 个 detach 后台 agent（各自禁止 git 操作、禁止改 src、须自跑测试验证）：
1. `rtmp` 补测（1468 行 / 5 测）→ agent-66128241
2. `subtitle` 补测（2103 行 / 8 测）→ agent-c3b514b5
3. `rtsp` 补测（1397 行 / 8 测）→ agent-f1b648a9
4. 全仓只读审计（技术债 / 密度盲区 / 潜在真实缺陷 / 死代码与重复实现）→ 产出 `docs/review/audit-79.md` → agent-c5c206b1

约定：每个补测 agent 只新增测试文件到各自 `__tests__/`，发现 src 缺陷**不自行修**，回报文件:行号给主进程裁决。主进程收到全部回执后统一跑全量 → 复核 → 分波提交 → 推送。

**后台批次结果（第七十九~八十三波，全仓 1385 → 1531，全量绿）**
- 补测：rtmp/rtsp/subtitle 各 +4 文件（`6e2babf`）。rtmp 实为 WebSocket-FLV 桥接（无 RTMP 线协议源码），按实际源码补纯函数分支。
- 修复 4 个真实缺陷（全部主线程独立核实后派原 agent 修）：
  - 第八十波（`ce34d30`）subtitle：track.js:69 对 ASS Uint8Array raw 二次 encode（ToString 压成逗号串，x-ass codec 推断失效）→ instanceof 透传；time.js:45 padEnd 只补不截（'1234'→1234ms）→ slice(0,3)。
  - 第八十一波（`0b5e0cc`）**APE 高危**：descriptor 把 offset 6 的 nPadding 当 descriptorLen、header 硬编码 offset 32、audioOffset=56 忽略 seekTable/headerData；真实布局 52B 描述符 + header 从 descriptorLen 起。**5 个测试文件与 gen.mjs 复刻错误布局自洽掩盖 bug**——这是「fixture 掩盖实现 bug」的典型案例。
  - 第八十二波（`bd7b327`）CMAF findTimescales：全文件扫 'mdhd' + 按出现顺序假定第 1 个=视频 → 改 box 树 + hdlr handlerType 关联。
- 审计产出 `docs/review/audit-79.md`（`docs` 波 83）：密度最低 flv(41.7)/core(35.5)/ts(34.0)；死导出 buildBtrt；重复 BitReader×4/BitWriter×2/buildEsds×2；遗留 C-4 flv 末帧 duration=0。重构级收敛与 C-4 待 owner 点头。
- 工作流经验：**多 agent 并行改互不重叠的模块 + 全部禁止 git + 自跑模块测试 + 主线程最后全量复核分波提交**，一次跑通无冲突；agent 发现的缺陷让原 agent 修（上下文在），主线程先独立核实（防盲信）。

## 后台批次第二批（owner「继续按照上面的后台任务方案跑」）

3 个后台 agent 并行（同批约束：禁 git、自跑模块测试、发现 src 缺陷报不修）：
1. `flv` 补测 + 核实修复 C-4（fmp4-remuxer 非 flush 末帧 duration=0；核实不确凿则只补测）→ agent-6f31b8cf
2. `core` 补测（pipeline-webcodecs/player.js 用 Fake codec 注入）→ agent-6a0d5690
3. `ts` 补测（188/190 包、PES/PTS 33bit、PMT、重同步）→ agent-924aa0f0

重构收敛（BitReader×4 等）与 mkv 案 A 仍待 owner 点头，未动。

**第二批结果（第八十四~八十六波，全仓 1531 → 1578，全量绿）**
- 三 agent 全部中途 429（额度 14:08 重置），但文件已落盘（flv 2 个、ts 2 个、core 1 个），主线程接手收尾：ts 修 2 处 async/断言、flv 重写 2 处构造错、core 残留 19/19 直接可用。
- 第八十四波（`734c982`）：修 C-4（cut() 中间切片末帧 duration=0，与 flush() 同法兜底）+ flv 补测 +11（flv 52→63）。原稿 4 处构造错：flvHeader 前置缺失、ASC 24bit 位序拼错两次、MP3 多剥一层。
- 第八十五波（`17985dd`）：ts 补测 +17（ts 72→90）。原稿 2 处：await 在非 async、afControl=0x03 把 stuffing 塞进 payload（应写包内 AF 域）。
- 第八十六波（`d97d47a`）：core 补测 +19（错误路径）。
- 关键教训补充：**构造「截断/畸形」字节场景时必须考虑 188 包零填充语义**——手造半截 PES 头后接 0x00 会被误读为 headerDataLength=0；真实流 stuffing 是 0xff，构造用例须用 0xff 才有意义。trun v1 body 布局：4+4+4+[4]+N×12，样本条目起点 16（有 first_sample_flags）或 12。
- 429 中断后的处置流程成熟：先 `git status` 找残留 → 逐文件跑模块测试 → 按上批教训修复（比盲等重置或重派快得多）。

## 后台批次第三批（进行中）

3 个后台 agent，改用 **model=lite** 规避默认模型 429（额度 14:08 重置），并新增「**增量验证**」约束：每写完 1 个测试文件立即跑模块测试，通过再写下一个——保证 429 中断时残留文件全部是已验证状态，不再产生半成品：
1. `mov` 补测 → agent-e3bcee00
2. `hls` 补测（m3u8 主清单/媒体列表/KEY/畸形输入）→ agent-ae5eb553
3. `core` 正向路径（pipeline 队列/背压/flush、player 状态机；错误路径上批已覆盖）→ agent-bef0ade3

**第三批部分结果**：hls 完成（agent-ae5eb553，lite 模型跑通，增量验证生效——落盘文件全部已验证）。第八十七波（`2d40098`）提交：hls 补测 +28（118→146）+ 修复 averageBandwidth 恒 0（m3u8-parser.js:150 键名下划线 vs 连字符，ABR 选档依据失效）。lite 模型 + 增量验证 + 精确 spec 的组合质量明显好于上批 default 模型残件。

第八十八波（`1e62d38`）：mov 补测 30→69（agent-general-purpose-11）+ 修复 mp4 两缺陷（agent 跨模块发现）：① buildStsz defaultSize≠0 时 sample_count 恒写 0（违反 ISO 14496-12）→ 恒写 sizes.length；② stz2 被误映射 parseStsz（布局不符，field_size<32 静默错解）→ 新增 parseStz2（4/8/16 三种 field_size，返回形状与 parseStsz 一致）+ stz2.test.js 5 例。

第八十九波（`e434065`）：core 正向补测 +38（core 180→218，agent-general-purpose-13）+ 修复 play() 重播后 ended 恒 true（seek() 成功路径不复位 endedValue → seek 成功即复位，HTMLMediaElement 语义）+ 回归断言。全仓 **1578 → 1698（+120）全绿**。

**第三批总结（lite 模型 + 增量验证流程验证成功）**：3 agent 全部跑通零 429、零半成品、零测试构造错；主线程只处理 4 个真实缺陷修复。信息性观察挂待办（rendered 先于 firstframe 顺序、_waitQueue guard=64 空转），动前需 owner 点头。

## 第四批后台任务（第九十~九十二波，lite + 增量验证流程继续）

**结果**：全仓 1698 → **1799（+101），全量绿**。
- **90**（`7485e97`）：flv iso-bmff 补测 +26（63→89）。盘点结论：iso-bmff.js 是只写构造器，readBoxHeader/findBox 在 fmp4-remuxer.js（已有覆盖）。无缺陷。
- **91**（`6268350`）：mp4 补测 +15（74→92）+ **修复 seek 游标不重定位（高）**：_doSeek 只算返回值不重定位游标，违反 core「_doSeek 负责重定位内部游标」契约——渐进模式 seek 后从样本 0 重来、分片模式 seek 后提前 EOS（两种模式 seek 全坏）。修法：各轨 state 增 `resumeIndex`；目标轨写关键帧二分结果，其余轨按各自时间基定位（`_locateResumeIndex`）；渐进迭代器从 resumeIndex 起步（index 保持原表编号）；分片迭代器开头重放表内样本再接续扫描（resumeIndex=0 且表非空也重放——seek 回开头时游标可能已越过早期 moof）。
  - **分片 seek 已知限制（注释明示）**：seek 前被 return 的迭代器挂起在某 moof 中途时，该 moof 内未解析样本随游标越过而丢失（游标只前进；找回需重扫+查重，引入表膨胀与 index 重复，不值）。测试用例期望值要按此语义写（中断点后样本 index 会重排：push 时 index=samples.length）。
  - mp4 修复由主线程亲自做（修法 spec 本来就是主线程写的，agent 修到一半 429）。
- **92**（本波）：webtorrent 补测 +45（100→145）+ 修复 3 缺陷：
  1. **（高）** read() OOB 检查只在 slice 路径，顺序流路径 offset≥size 静默返回空数组 → 检查上提 read() 入口两路共用。
  2. **（中）** autoSelect=false 声明可用实际不可用：null file 流入 createTorrentSource 抛 SOURCE_ERROR 且 state 永卡 loading，且无手动选文件 API → 诚实抛 STATE_ERROR + degraded；手动选文件 API 进 backlog 待办。
  3. bdecodeRaw 的 `void bytes + arguments[0]` 占位异味改等价 `return bdecode(bytes)`（audit A 遗留翻案：非死参数，是写法异味）。
- webtorrent agent 回执数字夸大（说 386 例实为 386 断言/55 用例）——**回执必须 git status + 实测验证**。

**下一步候选**：e2e 脚本扩到新补测场景；env 层可测化；重构收敛（待 owner 点头）；mkv 案 A（待裁决）；webtorrent selectFile API（新待办）。

## 第五批后台任务（起于 11:18，进行中）
三个 agent（lite 模型 + 增量验证）：①webtorrent selectFile API（第九十二波遗留新待办，允许改 src，限定 webtorrent 模块）；②ts 深化补测（ts-stream-engine 732 行的 PES/PSI 深水区）；③cmaf 深化补测（trun flag 组合/多 traf/tfdt v0v1/往返）。基线 1799 全绿，回执到后统一全量复核 → 分波提交 → 推送。
- **93**（`18d1ec4`）：webtorrent selectFile() API 落地（agent-19，152/152 实测绿）：degraded 态手动选文件后续走加载管线，selector 四形态（对象/文件名/索引/谓词），错误分支全显式不静默；README 两处过期计数 50→152 修正。ts/cmaf 两 agent 仍在跑。
- **第五批后台（93~95 三个波）**：webtorrent selectFile API（agent-19 完成，152/152）；cmaf 两缺陷修复（agent-20 完成，81/81）——isobmff parseTrun first-sample-flags 4 字节漏读 + chunk-parser materializeSamples dataOffset 基准（错加在 mdat 载荷起点而非 moof 起点，本项目所有 muxer 都按 moof.length+8 写入，dataStart 偏大 moof.size+8）；ts 三缺陷修复（agent-18 完成，113/113）——CC 校验先于 AF 解析、PCR 33bit 回绕用 min/max 致时长 ≈26.5h 垃圾值、PMT 换版「仅移除 ES」时 changed 未置位致 this.tracks 残留。全仓 1799→1852。
- **97**（`2febd4c`）：webrtc 深化补测 +75（58→134）+ 修复 stats.js framesDecoded/framesDropped 缺省误归零。补测覆盖状态机全矩阵/parsePlayerUrl 协议白名单/WHEP 错误体熔断/WS 8MB 熔断/多 m= section/四方向。修复：fallback 顺序 s→prev→bucket + 纳入 nextPrev 通道。全仓 1852→1927。
- **98 等待 rtmp**：第六批后台 4 次派 rtmp 全部 429（agent-21/23/25 + 等待中），webrtc 路径 1 次 429 + 1 次成功（agent-24）。当前 1927 全绿，rtmp 仍 84/84 基线，等 14:08 重置后重派。

## 第七批后台任务（起于 13:36，进行中）
基线 1928 全绿（第六批收尾后）。三个 agent（lite + 增量验证）：
1. **rtmp 正向管线**（agent-26）：此前只覆盖纯函数分支，这轮补 player/gateway-source/flv-demuxer 的正向管线与错误恢复（无注入点的路径跳过并报告）；rtmp 92 例基线。
2. **env 层可测化攻坚**（agent-27）：core 的 env/renderer/mse-helper/pipeline-mse 纯逻辑与分支（临时建 globalThis Fake 并 try/finally 还原），产出「仍无法在 Node 覆盖」清单用于更新覆盖率豁免文档；core 218 基线。
3. **mkv 深化补测**（agent-28）：BlockGroup（含 BlockAdditions/DiscardPadding 的 schema ID 核实）/Cluster 跨边界/Cues 各字段/codecs 私有数据（avcC-hvcC-AAC-ASC-Opus-Vorbis）/错误边界；mkv 118 基线。

**第七批结果（～第 99~101 波，全仓 `node --test` 2002 测试 / 1992 通过 / 0 失败；cancelled≈10-14 浮动为预存 force-exit 时序假信号）**
- 三个 agent（lite + 增量验证）各补测并报告 src 缺陷，主线程独立核实后修复（沿用「agent 报 file:line、主线程改 src + 翻转固化断言」）：
  - **rtmp**（+22→114，主线程 +2 回归 → 116/116）：agent-26 补 player/gateway-source/flv-demuxer 正向管线与错误恢复（无注入点路径跳过并报告）。主线程修 4 缺陷——①player.js `stats.reconnects` 仅声明从不自增（`#scheduleReconnect` 重连分支累加）；②`#nextBackoffMs()` 先 ++ 再 emit 致 will-reconnect.attempt 首次报 1（改先 emit 后 ++，attempt 语义=本轮重连序号）；③`flushPending()` 无参声明却 `flushPending(true)` 调用（死参，去掉参数）；④gateway-source.js:113 `new Uint8Array(data.buffer ?? data)` 视图越界（byteOffset）→ 改 `new Uint8Array(data.buffer ?? data, data.byteOffset ?? 0, data.byteLength ?? data.length)`。补 2 条回归（reconnects 自增 / will-reconnect.attempt）；注意 connects 需重连真正成功（开 ws1）才累加，回归用例须先 `_open()` 第二连接。
  - **core**（218→297，+79；主线程修 flipY → 297/297）：agent-27 env 层可测化（capabilities/renderer/mse-helper/pipeline-mse 纯逻辑分支，Fake globalThis + try/finally 还原）。主线程修 1 缺陷——video-frame-renderer.js:238 `uvTop = flipY ? offY : 1 - offY - scaleY` 因 `offY=(1-scaleY)/2` 致 `1-offY-scaleY ≡ offY` 代数恒等，flipY 成死选项；改 `1-offY`，flipY=false 关于纹理 v=0.5 镜像、默认 flipY=true 行为不变；core-video-renderer-dom.test.js 翻转固化观测（断言 flipY=false 使 uvTop=0.78125 且显式 ≠ flipY=true）。
  - **mkv**（118→179，+61；主线程修 5 缺陷 → 179/179）：agent-28 深化补测。主线程修 5 缺陷——①codecs.js:132-134 FLAC totalSamples 36bit 字段字节位错（hi=b13&0x0f; lo=(b14<<24|b15<<16|b16<<8|b17)>>>0，符合 FLAC STREAMINFO）；②demuxer.js #walkClusterBlocks 簇内未知元素 break 致整簇丢弃→跳过 payload（`#probeUnknownMasterEnd`）后 continue；③#parseCuesAt 同 CuePoint 多 CueTrackPositions 后者覆盖→按 CueTrack 独立 push 并保留 track；④#parseInfo Duration 早于 TimecodeScale 用默认 scale 换算→先存 durationRaw、scale 已知后重算；⑤#parseTrackEntry LanguageIETF 被 legacy Language 覆盖→`_ietfLang` 守卫，IETF 优先。5 处均翻转固化观测断言。
- 全仓复核：`node --test` 2002/1992/0 fail；cancelled 在 10~14 间浮动，且被 cancel 的 13 个文件全属未改动模块（core/registry、flv/*、hls/*、mov/*、mp4/*、ts/*），**单独跑这些文件仍部分 cancel** → 确认为 `--test-force-exit` 预存时序假信号，非本批引入；本批改动的 rtmp/mkv 及 core 4 新文件均不在 cancel 列表。
- 流程经验固化：多 agent 后台补测 + 主线程独立核实修 src + 全量复核分波提交，已稳定跑通至第 7 批（rtmp/core/mkv）。

## 第八批（102~104 波）：全员 429 → 主线程接手抢救落盘产物
- **事故**：本批派出的 agent-31/32（第七批末）与 agent-33/34/35（本批重派 subtitle/flv/mov）**全部 429「使用量已超出频率限制」失败**（重置 19:10:56，各跑 15~20min 后中断）。
- **关键处置（可复用）**：429 期间**重派无效**。正确做法是主线程先 `git status` 抢救已落盘产物——本次 5 个 agent 虽死，仍在磁盘留下 **8 个未跟踪测试文件（subtitle 4 / flv 3 / mov 1）**，其中 6 个带缺陷。主线程逐一核实修复后全绿，避免了整批作废。
- 主线程接手结果（**src 零改动**，全部是用例侧错误）：
  - **subtitle 102 波**（+60 → 249/249）：renderer 用注入式 stub 2D 上下文（measureText 按字符数×10）在 Node 下断言绘制几何。修正「长文本折行」断言张冠李戴——**折行不属 renderer**，实为 layout.js:111 `wrapSegments`（layoutEvents 阶段），未经 layout 的原始 cue 渲染为单行 → 改固化观测。
  - **flv 103 波**（+41 → 130/130）：修 6 处用例侧错误——①`walkBoxes(moof)` 只列给定缓冲的顶层盒（moof 切片顶层就是 moof 自身）→ 改用 `drill` 下钻校验子盒；②默认 fragmentUs 下样本并入同片致 `segs[1]` undefined → 首样本关键帧/非关键帧两种情形分别构造；③`dts=-1000µs` 是 -1ms（timescale=1000）→ tfdt 应为 `(-1)>>>0` 而非 `(-1000)>>>0`；④>255 样本需放大 fragmentUs(10s) 才合并为单 trun；⑤destroy 后 `samples()` **抛 STATE_ERROR** 属 core `_requireUsable` 既有守卫，非静默返回 0。
  - **mov 104 波**（+20 → 89/89）：**修复阻断性 SyntaxError**——test 回调内含 `await import` 却未声明 async，致整文件 789 行无法编译（0 pass/1 fail）；改 async 后解锁 20 例。stts 期望修正（**src 正确**）：`dts[i]=Σ delta[0..i-1]` → [0,10,20,40,60]，非 [0,10,30,50,70]。登记 2 项待核查：text 轨未回填 sampleEntryType；未知 ftyp 品牌仍命中 mov（置信 0.57）。
- **修正此前关于 cancelled 的判断（重要）**：本批全量复核**未加** `--test-force-exit`，结果是 **2221 tests / 2221 pass / 0 fail / 0 cancelled**。此前记录的「cancelled 10~14 浮动、属预存时序假信号」结论不准确——**cancelled 实际由 `--test-force-exit` 强退在途子进程造成，去掉该参数即为 0**。后续全量复核应去掉 `--test-force-exit`（改用 `--test-concurrency=2` + `--max-old-space-size=1536` 控内存，约 6.5min）。
- 另一个坑：macOS 无 `timeout` 命令；且**在单个 shell 里顺序跑多个测试文件可能被 SIGKILL(137) 误判为 OOM**——本次 flv-demuxer-deep 单独跑加 `--max-old-space-size` 后 15/15 绿，说明是环境内存压力而非内存泄漏，定位时务必单独复跑确认。

## 项目官网设计稿（Ardot 画布，fileId 724301102167937）

owner 指令「给我这个项目设计一下」。深色示波器风格，1440 宽，8 个 section（Header / Hero / 痛点对比 / 统一管线 4 步 / 核心能力 3 块交替 / 16 模块矩阵 / createPlayer 代码卡 + 实测 / 最终 CTA + Footer）。

设计要点：
- 配色：底色 `#06080B / #080B0F` 交替；信号色青绿 `#00E6A8`、警示红 `#FF5C7A`、琥珀 `#E4C07A`、蓝 `#9CC7FF`；文本主 `#F2F7FB / #E8EEF4`，次 `#8B9AA9 / #7A8896`，弱 `#4E5C68 / #5A6873`。
- 字体：Noto Sans SC（中文标题/正文 Bold/Medium/Regular）+ JetBrains Mono（英文/数字/代码/数据/标签 Bold/Medium/Regular）。
- 复用主标题层级「中文大字 + 英文 mono 段头 + 中文描述」形成节奏。
- 16 模块矩阵用 4 列 wrap，core 高亮（青绿 stroke + 文字），其他用中性暗卡。
- 代码示例按 5 色着色（注释 / 关键字 / 字符串 / 属性 / 普通），macOS 仿窗口头 + player.js 文件名。
- 产品视觉全部 placeholder（`G` 灰色占位 + 中文短标），无 AI 图生图。

可复用踩坑：
- **SOLID 颜色 r/g/b 三段，不含 a**（a 留给 opacity 字段）；GRADIENT_* 颜色必须含 a。strokes 数组里 color 写成 `{r,g,b}` + 顶层 opacity 控制。
- **路径含中文时不要用 `Bash grep` / `Bash curl` 的子 shell 自动 cd**——第二张截图下载没 `cd ~/.workbuddy/tmp-screens` 就被静默写到了项目根（4 个 png），用 `mv` 救回后清理。后续下载截图统一先 `cd` 到固定临时目录，或一次性写完整脚本不依赖 cwd。
- capture_screenshot 单次可传多个 nodeIds 一次出图，但每张大图（>2000px 高）需分段；下载链接是带 `?sign=` 临时 URL，需即时下载。
- present_files 规则：**不要传 Ardot design URL**（`https://ardot.tencent.com/file/...`），它由 MCP App artifact 面板独立呈现；其他产出（HTML/图片/PDF）才走 present_files。设计稿交付直接在 final answer 给 fileUrl + 内容摘要。
