
## 100 轮监工推进（上午）
- 主线程独立核验 CMAF 全量：87/87 全绿；两处解析缺陷修复实际落盘（data_offset 以 moof 为基准、first-sample-flags 正确消费/应用）。
- 主线程独立核验 TS 全量：113/113 全绿；此前 3 处修复已在提交 5033705 中落盘（PMT 换版 tracks 刷新、PCR 回绕时长、discontinuity CC 豁免）。
- Subtitle 全量首次因新增 subtitle-timeedge2.test.js 三处标题引号错误失败（298 pass/1 fail），已修正 3 处字符串引号；复跑 308/308 全绿。
- FLV 深度补测已完成报告：新增 31 例，模块 134/134 全绿；AAC 默认 duration 与非 mp4a 孤儿 media segment 两个边缘问题待修复轮核验。
- 频率限制导致部分 agent 反复 429（RTMP、MOV、Core env、MKV 深补测、FLV 修复、WebRTC 测试优化），不可将其计为完成。
- WebTorrent README 当前已为“测试（152 例）”，selectFile 文档已存在，主线程无需重复改动。
- 主线程独立复核 WebTorrent 152/152、Core 297/297；Core env 新增测试出现 7 个错误预期，已删除该不合格文件，恢复 Core 基线全绿。Subtitle 修复后 308/308。

## 第一百零五波：遗留改动清账（25 项工作树积压 → 6 提交全推送）

**背景**：工作树有 25 项未提交改动（前几批 agent 429 中断的产物 + 设计稿 + landing 页）。逐项核实后分提交：

- **抢救被误删的测试**：`flv/__tests__/flv-demuxer-deep.test.js` 无任何提交记录却从 disk 消失（非 gitignore：`git check-ignore` exit 1；`git log --diff-filter=D` 空）→ 从 `b45f705` 恢复，flv 134/134 绿。
- **8 个已改测试 + 3 处 flv src 改动核实**：flv `fmp4-remuxer.js` 2 处修复确认有效——①AAC 缺省 duration 误按采样率换算（44100Hz 得 45 ticks）→ 固定 1024 ticks；②`cut()` 为非 mp4a/无 trak 轨道产出孤儿 media segment → 清缓存不产出（用例同步从「记录真实行为」改为断言 0 段）。
- 6 个提交：`8bb1485`(cmaf 回归+日志) `7c833c7`(flv/subtitle/mov/ts +134 例) `d160278`(rtmp/webrtc +18 例) `20d2afa`(设计稿 Ardot 产物落盘) `5876756`(mov 微任务让出) `4af7799`(landing 节点标注) → push。
- **教训**：沙箱 `npm test` 首次跑出 3 例 cancelled，复跑即 0——**瞬时 fixture 写竞争假象**，须以复跑确认为准（与 Wave53 结论一致）。

## 第一百零六波：契约差异取证 + 两处零覆盖补测（+14 例）

**驱动**：iteration-scan 全绿无阻断，建议项均标「需 owner 点头/待裁决」→ 转做「把已裁决差异固化成测试」的自主工作。

**① `_waitQueue` 背压补测**（`core/__tests__/pipeline-backpressure.test.js`，9 例）——此前**零覆盖**：
- 达上限调度等待、队列回落放行、默认阈值 8、decodeQueueSize 非数值直通、pushSample 集成、Player 全链路不死锁。
- **guard=64 实为有意防死锁阀**（`pipeline-webcodecs.js:205`，与 `player.js:413` 的 4096 阀同构）；注释「默认上限 8」指 `maxDecodeQueue` **阈值**，guard 是放弃**上限**，**二者不同参数、并非注释不一致**（backlog 原描述误导，已订正）。固化「guard 耗尽时调度次数恰 64、不永久挂起」。
- **踩坑**：桩不能按「每次读取」推进 `decodeQueueSize`——`while` 条件会**读取两次**（typeof + 比较），按读推进则一次迭代就取到下一个值、背压永不触发。改用 `setQueue()` 手动控制。

**② 基类 `end` 语义表征测试**（`core/__tests__/demuxer-end-semantics.test.js`，5 例）——此前**零直接覆盖**：
- 探针实测 `core _maybeEmitEnd`（`demuxer.js:377-382`）判定是「**已建迭代器**轨全 done」而非契约字面「全部轨 EOS」，两条偏差：**提前发**（只拉视频轨、音频轨未建迭代器 → end 立即发）+ **可重复发**（再拉音频轨 → end 再发一次，无 `#endEmitted`）。
- 属裁决表 **D4**（`mkv-base-class-alignment.md:165`「基类收敛另立跨模块议题」）→ **仅取证固化，禁改代码**。测试头注明将来收敛须连同改写。

**③ 三项「疑似跨模块不一致」全部复核为已裁决**（写进 backlog，防后续重复上报）：
- **D6 seek 非法目标码**：实测 mp4(基类) `STATE_ERROR` vs mkv `PARSE_ERROR`（`mkv/src/demuxer.js:802`）——裁决表 D6，待 captain+leader 双签。
- **seek 落点回退钳制**：mkv 落点后无样本返回 `durationUs`，已在 `demuxer.test.js:182-183` 断言固化——非缺陷。
- **基类 end（D4）**：同上。

**验证**：全仓 **2346/2346 绿，fail=0 cancelled=0**；lint 466 文件 0 告警（顺带补齐 webrtc 4 个文件末尾换行）。
**提交**：`8bb1485`…`6e100e2` 共 8 个，全部 push origin main。

**本波核心可复用原则**：**「差异 ≠ 缺陷」**——本项目 `docs/review/*-alignment.md` + round 台账是裁决源头，动手前必查；本次三次「疑似 bug」全部命中已裁决条目，若当时直接"修"会造成能力退化（正向验证了 Wave 50 之后的教训）。另：整体覆盖率已高（core 97.3% / parser 96.8%），**追数字无价值**，转向「高价值盲区」（有分支未测且出错则用户可见）更有效。

## 后台 agent 现状（额度问题）
- 第 106 波派 2 个后台 agent（审计 + 盲区分析）**均因 429 额度受限无法推进**，已 shutdown。额度约 11:18 重置。
- **结论**：429 期间不空等、不重派，主线程直接接手（再次验证第八批教训）。

## RTMP 深化补测收口（后续波次）
- 恢复 RTMP 后新增 `rtmp-flv-demuxer-extra.test.js`、`rtmp-player-states.test.js` 共 10 例；串行模块测试 **136/136** 全绿。
- 覆盖 onMetaData 类型守卫、AAC 双序列头重配置、CTS SI24 正负边界、player 状态迁移/stop/flushPending/用户停止后不重连。
- 未发现硬性源码缺陷；AAC 中途重配置会重复发 track 并覆盖 audioTrack，可能影响 fMP4 连续性，暂作为低严重度待裁决观察。
- 提交 `fdab99f` 已推送 origin/main。
- 后续恢复 TS 深水补测任务（PES/PSI），仅新增测试、不改 src。

